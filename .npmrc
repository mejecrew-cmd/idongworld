# 📁 .npmrc — pnpm 옵션
# ───────────────────────────────────────────────

# peerDependency 자동 설치 (React·MUI 등 peer 누락 방지)
auto-install-peers=true

# 엄격한 격리 (한 패키지가 다른 패키지의 의존성에 직접 접근 X)
shamefully-hoist=false

# 다음 작업 메모 2026-06-15 M1

이 문서는 `.cloud/71_roadmap_2026-06_260612.md`의 두 번째 마일스톤 `M1. 마이섬 15구역 슬롯 전환` 실행 보드다.

목표는 마이섬을 최신 6/11 사이트맵의 AREA-01~15 고정 구조로 맞추고, 기존 무한 가변 Aidong 구역 UI를 13개 편입 슬롯 구조로 전환하는 것이다.

## 기준

- 상위 로드맵: `.cloud/71_roadmap_2026-06_260612.md`
- 직전 실행 보드: `.cloud/77_next_work_2026-06-14_week1_b.md`
- frontend route audit: `.cloud/80_frontend_route_audit_2026-06-14.md`
- backend module 충돌 지도: `.cloud/81_backend_module_conflict_map_2026-06-14.md`
- 고정 15구역 슬롯 전환 결정: `.cloud/82_fixed_15_area_slot_decision_2026-06-14.md`
- 사이트맵 SoT: `.cloud/개발기획서260605/사이트맵_260611_최종확정.md`

## M0 잔여 위험

- `.cloud/77_next_work_2026-06-14_week1_b.md`의 7번 `규칙 문서 갱신`은 아직 미완료다.
- M1 구현 중에도 규칙 문서와 충돌하지 않도록 82번 결정 문서를 우선 기준으로 따른다.
- 7번 규칙 갱신은 M1 착수 직전 또는 M1 1번 완료 직후 별도 작업으로 닫는 것이 좋다.

## 이번 보드의 목표

M1 완료 시 다음 상태가 되어야 한다.

- `packages/frontend/src/data/zones.ts`가 최신 AREA-01~15 순서와 의미를 따른다.
- backend `myIslandStates`에 `zoneSlots` 구조가 추가된다.
- AREA-02 항구와 AREA-13 숙소는 anchor slot으로 잠긴다.
- 13개 fillable slot에만 Aidong 편입이 가능하다.
- 기존 `dynamicAidongZones`는 삭제하지 않고 migration/compat 입력으로 유지된다.
- `IslandFullMapScreen`은 15구역 카드와 13 편입 슬롯 상태를 보여준다.

## 1. AREA-01~15 static data 전환

- [x] `packages/frontend/src/data/zones.ts`의 기존 `ZONES`를 최신 AREA-01~15 순서로 교체한다.
- [x] `Zone` 타입에 `areaNo`, `kind`, `legacyRoute`, `lockedReason`를 추가한다.
- [x] AREA-02 항구와 AREA-13 숙소를 `kind: 'anchor'`로 둔다.
- [x] 기존 `/island/harbor`, `/island/lodge`, `/island/garden`, `/island/oasis`, `/island/memory`, `/island/mine` 호환 route를 `legacyRoute` 또는 별도 mapping 후보로 남긴다.
- [x] `/dev/catalog` 또는 zone tab에서 새 필드를 깨지지 않게 확인한다.

처리 결과:

- `packages/frontend/src/data/zones.ts`를 최신 AREA-01~15 순서로 교체했다.
- `Zone` 타입에 `areaNo`, `kind`, `legacyRoute`, `lockedReason`를 추가했다.
- AREA-02 `harbor`, AREA-13 `lodge`를 anchor slot으로 지정했다.
- 현재 구현 route가 있는 항구, 숙소, 기억, 오아시스, 도전, 정원은 `route`와 `legacyRoute`를 유지했다.
- 아직 `/island/area/*` route는 구현하지 않았으므로, 공식 AREA route 전환은 7번 route alias 단계에서 처리한다.

완료 기준:

- frontend static zone data가 82번 결정 문서의 AREA-01~15 표와 일치한다.

## 2. backend `zoneSlots` model/default/spec 추가

- [x] `packages/backend/src/models/MyIslandStateModel.ts`에 `MyIslandZoneSlot` 타입과 `zoneSlots` 필드를 추가한다.
- [x] `packages/backend/src/repositories/moduleDefaults.ts`에 15개 기본 slot을 추가한다.
- [x] `packages/backend/src/modules/modelSpecs.ts`의 `my-island.ownedFields`에 `zoneSlots`를 추가한다.
- [x] memory repository와 Mongo repository에서 기존 document가 `zoneSlots` 없이 읽혀도 기본값을 보강하는지 확인한다.
- [x] `dynamicAidongZones`는 제거하지 않는다.

처리 결과:

- `MyIslandStateModel.ts`에 `MyIslandZoneSlot`과 `zoneSlots` 필드를 추가했다.
- `moduleDefaults.ts`에 AREA-01~15 기본 slot 생성 함수를 추가했다.
- AREA-02 `harbor`, AREA-13 `lodge`는 기본 상태 `locked` anchor로 생성된다.
- 나머지 13개 구역은 기본 상태 `empty` fillable로 생성된다.
- `modelSpecs.ts`의 `my-island.ownedFields`에 `zoneSlots`를 추가했다.
- memory/Mongo dedicated repository는 기존 document를 반환할 때 default와 병합해서, `zoneSlots`가 없는 기존 문서도 읽기 결과에서는 기본 slot을 가진다.
- `dynamicAidongZones`는 compat 필드로 그대로 유지했다.

완료 기준:

- 새 유저와 기존 유저 모두 `myIslandStates.zoneSlots`를 읽을 수 있다.

## 3. slot service와 validation 구현

- [x] `packages/backend/src/modules/my-island/service.ts`에 slot 조회 helper를 추가한다.
- [x] `incorporateSlot` service를 추가한다.
- [x] `releaseSlot` service를 추가한다.
- [x] 필요하면 `moveSlot` service를 추가한다.
- [x] AREA-02, AREA-13에는 편입할 수 없게 막는다.
- [x] 영입되지 않은 Aidong은 편입할 수 없게 막는다.
- [x] 같은 Aidong이 여러 fillable slot에 중복 편입되지 않게 막는다.
- [x] 기존 `openDynamicAidongZone`은 compat로 유지한다.

처리 결과:

- `packages/backend/src/modules/my-island/service.ts`에 `listZoneSlots`, `incorporateSlot`, `releaseSlot`, `moveSlot` service를 추가했다.
- `getZoneSlots` helper는 기존 document에 일부 slot이 빠져 있어도 AREA-01~15 기본값과 병합해 반환한다.
- `incorporateSlot`은 `my-aidong`의 `assertAidongRecruited`를 통해 영입된 Aidong만 편입할 수 있게 막는다.
- AREA-02 항구와 AREA-13 숙소 같은 anchor slot은 `zone_slot_anchor_locked`로 실패한다.
- 이미 다른 slot에 편입된 Aidong은 `aidong_already_in_zone_slot`로 실패한다.
- 이미 다른 Aidong이 들어간 slot은 `zone_slot_occupied`로 실패한다.
- `releaseSlot`은 빈 slot과 anchor slot을 실패 처리한다.
- `moveSlot`은 fillable slot 사이의 이동만 허용하고, 도착 slot이 비어 있어야 한다.
- 기존 `openDynamicAidongZone`, `updateDynamicAidongZone`은 compat API용 service로 유지했다.

완료 기준:

- backend service 단에서 13개 fillable slot 편입 규칙이 강제된다.

## 4. slot API route 추가

- [x] `packages/backend/src/modules/my-island/routes.ts`에 `GET /slots`를 추가한다.
- [x] `POST /slots/incorporate`를 추가한다.
- [x] `POST /slots/release`를 추가한다.
- [x] 구현한다면 `POST /slots/move`를 추가한다.
- [x] route response는 갱신된 `my-island` state 또는 `zoneSlots`를 반환하도록 통일한다.
- [x] 기존 `/dynamic-zones/open`, `/dynamic-zones/update`는 삭제하지 않고 compat route로 유지한다.

처리 결과:

- `GET /api/modules/my-island/slots`를 추가했다.
- `POST /api/modules/my-island/slots/incorporate`를 추가했다.
- `POST /api/modules/my-island/slots/release`를 추가했다.
- `POST /api/modules/my-island/slots/move`를 추가했다.
- slot route 응답은 `{ ok, state, zoneSlots }` 형식으로 맞췄다.
- 기존 `/dynamic-zones/open`, `/dynamic-zones/update`는 삭제하지 않고 compat route로 유지했다.

완료 기준:

- frontend가 dedicated API로 15구역 slot 상태를 읽고 편입/해제할 수 있다.

## 5. frontend API와 store facade 연결

- [x] `packages/frontend/src/lib/api.ts`에 slot API client를 추가한다.
- [x] `packages/frontend/src/stores/userStore.ts` 또는 facade에 `zoneSlots` 타입과 최소 setter를 추가한다.
- [x] `packages/frontend/src/lib/syncStore.ts`의 my-island sync key에 `zoneSlots`를 추가한다.
- [x] 기존 `dynamicAidongZones` selector는 즉시 삭제하지 않고 compat selector로 낮춘다.

처리 결과:

- `packages/frontend/src/lib/api.ts`에 `MyIslandZoneSlot` 타입과 `listMyIslandZoneSlots`, `incorporateMyIslandSlot`, `releaseMyIslandSlot`, `moveMyIslandSlot` client를 추가했다.
- `packages/frontend/src/stores/userStore.ts`에 `zoneSlots` 상태, `MyIslandZoneSlot` 타입, `setZoneSlots` action을 추가했다.
- `packages/frontend/src/lib/storeFacades.ts`에 `useZoneSlots`, `getZoneSlots`, `setZoneSlots` facade를 추가했다.
- `packages/frontend/src/lib/syncStore.ts`의 `SYNCED_KEYS`, `MY_ISLAND_KEYS`에 `zoneSlots`를 추가했다.
- 기존 `dynamicAidongZones` selector/action은 삭제하지 않고 compat로 유지했다.

완료 기준:

- frontend store가 `zoneSlots`를 받아 화면에 넘길 수 있다.

## 6. `IslandFullMapScreen` 15구역 슬롯 UI 전환

- [x] 기존 `ZONES` 카드가 AREA-01~15 순서로 표시되게 한다.
- [x] 각 카드에 `areaNo`, anchor/fillable, 현재 occupant 상태를 표시한다.
- [x] AREA-02 항구는 항구 진입 버튼을 표시한다.
- [x] AREA-13 숙소는 숙소 진입 버튼을 표시한다.
- [x] fillable slot은 비어 있음, 편입됨, 활성, 대기 상태를 구분해 표시한다.
- [x] 기존 `Aidong 전용 구역 관리` section은 13개 fillable slot 관리 UI로 흡수한다.

처리 결과:

- `packages/frontend/src/screens/IslandFullMapScreen.tsx`의 15구역 카드 제목을 `AREA-01`~`AREA-15` 기준으로 표시하도록 바꿨다.
- 각 구역 카드에 anchor 또는 빈 슬롯/편입됨 상태 chip을 표시했다.
- 미니게임 매핑을 최신 zone id인 `growth-garden`, `memory-forest`, `challenge-cliff`, `oasis` 기준으로 보정했다.
- 기존 `Aidong 전용 구역 관리` section을 `13 편입 슬롯 관리` section으로 바꿨다.
- 13개 fillable slot을 카드로 표시하고, occupant가 있으면 `편입됨`, 없으면 `빈 슬롯`으로 표시한다.
- 아직 편입 관리 UI는 4번 API와 5번 store 연결만 마친 상태라 버튼을 disabled로 두고, 실제 선택/편입 UI는 후속 작업으로 남겼다.

완료 기준:

- 마이섬 화면에서 Aidong 구역이 무한 list가 아니라 13개 fillable slot으로 보인다.

## 7. route alias와 placeholder 점검

- [x] `/island/area/02`와 `/island/harbor`의 관계를 구현 또는 문서화한다.
- [x] `/island/area/13`과 `/island/lodge`의 관계를 구현 또는 문서화한다.
- [x] 기존 `/island/garden`, `/island/oasis`, `/island/memory`, `/island/mine`은 삭제하지 않고 alias/redirect/compat 후보로 둔다.
- [x] M1 범위를 넘는 `/island/area/01~15` 개별 화면은 placeholder로 처리할지 결정한다.

처리 결과:

- `packages/frontend/src/App.tsx`에 `/island/area/02` -> `/island/harbor`, `/island/area/13` -> `/island/lodge` redirect를 추가했다.
- 이미 구현 화면이 있는 AREA-03, AREA-06, AREA-12, AREA-14는 각각 `/island/memory`, `/island/oasis`, `/island/mine`, `/island/garden`으로 redirect한다.
- 아직 전용 화면이 없는 AREA-01, AREA-04, AREA-05, AREA-07, AREA-08, AREA-09, AREA-10, AREA-11, AREA-15는 `IslandAreaPlaceholderScene`으로 받는다.
- 기존 `/island/garden`, `/island/oasis`, `/island/memory`, `/island/mine` route는 삭제하지 않고 compat route로 유지했다.
- AREA-05는 최신 기획상 `자신감 폭포` placeholder로 두고, 광산 구현은 현재 AREA-12 `도전의 절벽`에 연결했다.

완료 기준:

- route audit의 M03 관련 충돌이 구현 또는 명확한 compat 정책으로 닫힌다.

## 8. 테스트와 smoke 보강

- [x] backend persistence test에 `zoneSlots` default 생성 케이스를 추가한다.
- [x] anchor slot 편입 실패 케이스를 추가한다.
- [x] 같은 Aidong 중복 편입 실패 케이스를 추가한다.
- [x] release 또는 move의 최소 성공 케이스를 추가한다.
- [x] frontend smoke에서 마이섬 진입과 AREA-01~15 렌더링을 확인한다.

처리 결과:

- `packages/backend/src/backendPersistence.test.ts`에 `zoneSlots` 기본값 생성 테스트를 추가했다.
- AREA-01~15 전체 key, AREA-02 항구 anchor 잠금, AREA-13 숙소 anchor 잠금, AREA-14 fillable 기본값을 검증한다.
- `incorporateSlot`, `moveSlot`, `releaseSlot` service 테스트를 추가했다.
- 영입되지 않은 Aidong 편입 실패, anchor slot 편입 실패, 같은 Aidong 중복 편입 실패, 점유 slot 편입 실패, move 성공, release 성공, 빈 slot release 실패를 검증한다.
- 현재 CSV 기준과 어긋나던 기존 destination customs 기대값도 함께 현행화했다. `shell-fragment-to-ship`은 현재 1:1 이동 규칙이다.
- `packages/frontend/scripts/check-state-route-runtime.mjs`에 `App.tsx`의 AREA route wiring과 `zones.ts`의 AREA-01~15 static data 검사를 추가했다.
- runtime smoke는 backend/frontend dev server가 떠 있어야 실행된다. 현재 서버가 꺼져 있어 `ECONNREFUSED`로 실행 확인은 보류했고, 타입 체크와 backend Vitest는 통과했다.

완료 기준:

- `zoneSlots` 기본값, anchor 잠금, 중복 편입 방지가 자동 검증된다.

## 9. 문서와 changelog 정리

- [x] `.cloud/82_fixed_15_area_slot_decision_2026-06-14.md`의 M1 구현 체크리스트를 완료 상태에 맞게 갱신한다.
- [x] `.cloud/77_next_work_2026-06-14_week1_b.md`의 7번 규칙 문서 갱신이 남아 있다면 별도 위험으로 유지한다.
- [x] `.cloud/71_roadmap_2026-06_260612.md`의 최신 실행 보드를 이 문서로 바꾼다.
- [x] 수정한 한글 문서의 깨짐 문자를 검사한다.

처리 결과:

- `.cloud/82_fixed_15_area_slot_decision_2026-06-14.md`에 M1 route/test 구현 결과와 남은 `route-neighbor/encounter/accept` 분리 작업의 위치를 명확히 남겼다.
- `.cloud/77_next_work_2026-06-14_week1_b.md`의 7번 규칙 문서 갱신은 이번 M1 구현 범위와 분리된 문서 debt로 유지했다.
- `.cloud/71_roadmap_2026-06_260612.md`의 최신 실행 보드가 이미 `.cloud/83_next_work_2026-06-15_m1.md`로 되어 있음을 확인했다.
- 이번 보드의 검증 항목과 변경 기록을 M1 완료 상태에 맞게 갱신했다.

완료 기준:

- M1 구현 결과와 문서가 어긋나지 않는다.
## 검증

- [x] `pnpm exec vitest run packages/backend/src/backendPersistence.test.ts`를 실행한다.
- [x] `pnpm --filter backend typecheck`와 `pnpm --filter frontend typecheck`를 실행한다.
- [x] 수정한 한글 문서에서 깨짐 문자를 검사한다.

검증 결과:

- backend persistence test: 30개 통과.
- backend typecheck: 통과.
- frontend typecheck: 통과.
- frontend runtime smoke는 backend/frontend dev server가 필요하다. 서버가 꺼진 현재 환경에서는 `ECONNREFUSED`가 나므로, 서버 실행 상태에서 재확인한다.
- 깨짐 문자 검사: 통과.
## 변경 기록

- **2026-06-12**: M1 시작 보드를 작성했다. 핵심 순서는 AREA-01~15 static data 전환, `zoneSlots` backend model/default/spec 추가, slot API, frontend store/facade, `IslandFullMapScreen` 전환, route alias 점검, 테스트 보강이다.
- **2026-06-12**: 1번 AREA-01~15 static data 전환을 완료했다. `zones.ts`의 `ZONES`를 최신 AREA 순서로 교체하고 `areaNo`, `kind`, `legacyRoute`, `lockedReason` 필드를 추가했다. AREA-02와 AREA-13은 anchor로 잠갔다.
- **2026-06-12**: 2번 backend `zoneSlots` model/default/spec 추가를 완료했다. `myIslandStates.zoneSlots` 타입과 schema, AREA-01~15 기본 slot, model spec ownership, 기존 문서 default 보강을 추가했다.
- **2026-06-12**: 3번 slot service와 validation 구현을 완료했다. `listZoneSlots`, `incorporateSlot`, `releaseSlot`, `moveSlot`를 추가하고 anchor 잠금, 영입 여부, 중복 편입, 빈 slot release, 도착 slot 점유 검증을 service 단에 넣었다.
- **2026-06-12**: 4번 slot API route 추가를 완료했다. `/api/modules/my-island/slots`, `/slots/incorporate`, `/slots/release`, `/slots/move`를 열고 기존 dynamic zone route는 compat로 유지했다.
- **2026-06-12**: 5번 frontend API와 store facade 연결을 완료했다. frontend API client, Zustand `zoneSlots` 상태와 setter, facade selector/action, sync key를 추가하고 `dynamicAidongZones`는 compat로 유지했다.
- **2026-06-12**: 6번 `IslandFullMapScreen` 15구역 슬롯 UI 전환을 완료했다. 15구역 카드에 AREA 번호와 slot 상태를 표시하고, 기존 dynamic zone section을 13개 fillable slot 관리 UI로 흡수했다.
- **2026-06-12**: 7번 route alias와 placeholder 점검을 완료했다. `/island/area/02`, `/island/area/13`은 항구/숙소로 redirect하고, 구현된 zone route는 compat redirect, 미구현 AREA는 placeholder 화면으로 받게 했다.
- **2026-06-12**: 8번 테스트와 smoke 보강을 완료했다. backend persistence test에 zoneSlots 기본값, anchor 잠금, 중복 편입, move/release 케이스를 추가하고, frontend runtime smoke에 AREA route/data 정합 검사를 추가했다.
- **2026-06-12**: 9번 문서와 changelog 정리를 완료했다. 82번 결정 문서, 77번 잔여 규칙 debt, 71번 최신 실행 보드 표기, 검증 기록을 M1 완료 상태에 맞게 정리했다.

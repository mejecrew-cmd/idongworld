# 다음 작업 메모 2026-06-08

> 2026-06-12 기준 이 문서는 6/8 기획 변경 대응 이력으로 보존한다. 최신 실행 보드는 `.cloud/72_next_work_2026-06-12_week1_a.md`를 따른다.

이 문서는 `.cloud/기획변경회의에따른기획변경가이드.md`를 기준으로 2026-06-05 이전 작업 보드를 재정렬한 후속 작업 목록이다.

이전 작업 보드 `.cloud/49_next_work_2026-06-05_2.md`는 2026-06-08 기준 취소 처리한다. 1~11번까지 완료된 구현 이력은 보존하되, 다음 구현 순서는 이 문서를 따른다.

## 전제

- 2026-06-05 회의 가이드는 최종 SoT가 아니라 방향 전환 입력이다.
- 월요일 재논의 전까지는 대규모 삭제보다 충돌 정리, 문서 현행화, feature flag, fallback 중심으로 움직인다.
- "통으로"는 백엔드 경계를 없애자는 뜻이 아니라, Phase 1 게임 루프에서 세관과 모듈화를 유저에게 강제하지 않는다는 뜻으로 해석한다.
- 이미 분리된 backend route/service/repository, Mongo collection, module package 구조는 유지한다.
- 세관 강제 UI/UX는 Phase 1 core loop에서 제거한다.
- customs backend, customsLogs, resource adapter, module route/service/repository 경계는 기술 자산으로 유지한다.
- 모듈화는 계속 유지한다. 단, 사용자에게 세관과 모듈 경계를 핵심 플레이 규칙으로 강제하지 않는다.
- customs는 외부 IP 콜라보나 연말 리팩토링 후보로 보존하되, 기본 플레이 동선에서는 비강제 상태로 둔다.
- 배/숙소/도착 섬/세관 관련 최근 구현은 폐기하지 않고, 새 기획에 맞춰 숨김, 축소, 재배치, 내부 인프라화 여부를 결정한다.

## 지금 바로 멈출 작업

- 세관 팝업을 핵심 플레이 루프의 필수 gate로 더 강화하는 작업.
- 세관 팝업, 세관 버튼, 모듈 이탈 세관 gate를 신규 core UX에 노출하는 작업.
- destination island local resource를 customs 없이는 나갈 수 없게 만드는 UX 확장.
- 배 인벤토리와 숙소 인벤토리를 엄격히 분리하는 추가 작업.
- 아이동 섬을 별도 일회성 목적지로 확장하는 작업.
- 30칸 주사위 항해 보드를 확정 전제로 삼는 밸런스/화면 고도화.
- 외부 IP 콜라보를 전제로 한 전용 아이템/완전 모듈화 작업.

## 새 작업 순서

### 1. SoT 재정렬과 충돌 지도 작성

- [x] `.cloud/기획변경회의에따른기획변경가이드.md`를 기준으로 현재 구현/문서 충돌 목록을 만든다.
- [x] `.cloud/00_project_overview.md`, `.cloud/20_module_rules.md`, `.cloud/30_backend_db_rules.md`, `.cloud/40_frontend_rules.md`에서 세관/모듈화 강제 표현을 "Phase 1 보류"로 낮춘다.
- [x] `packages/modules/BACKEND_GUIDE.md`, `WORLD_SCOPE_GUIDE.md`, `ITEM_CATALOG_GUIDE.md`에서 Phase 1 핵심 루프와 Post/리팩토링 후보를 구분한다.
- [x] 취소된 2026-06-05 보드의 완료 작업 중 유지/숨김/재사용/폐기 후보를 표로 분류한다.
- [x] 한글 깨짐이 없도록 UTF-8 기준으로 검수한다.

구현 결과:

- `.cloud/52_plan_change_conflict_map_2026-06-08.md`를 추가해 기존 구현/문서와 새 기획 방향의 충돌을 정리했다.
- 세관, destination island, ship/lodge inventory, route landing, 항해 보드, 숙소, zone action, codex, smoke 기준을 유지/축소/숨김/재사용/보류/미결로 분류했다.
- `.cloud/00_project_overview.md`, `.cloud/20_module_rules.md`, `.cloud/30_backend_db_rules.md`, `.cloud/40_frontend_rules.md`에 2026-06-08 기획 변경 반영 안내를 추가했다.
- `packages/modules/BACKEND_GUIDE.md`, `packages/modules/WORLD_SCOPE_GUIDE.md`, `packages/modules/ITEM_CATALOG_GUIDE.md`에 Phase 1 기준과 customs/모듈화 보류 기준을 추가했다.
- 세관과 모듈 경계는 삭제가 아니라 기술 자산으로 유지하고, 사용자 핵심 루프 강제만 보류하는 것으로 정리했다.
- 검증: 수정 파일 전체에서 한글 깨짐 대체 문자가 없는지 확인했다.

완료 기준:

- 새 기획에서 유지할 코드와 당장 숨길 코드를 구분한 문서가 생긴다.
- 세관/모듈화는 "삭제"가 아니라 "Phase 1 UX 강제 보류"로 표현된다.

### 2. Core Loop 재정의

- [x] 현재 구현된 루프를 회의 기준 루프로 다시 쓴다.
- [x] 새 루프를 `항해 -> 아이동 만남/재료 수집 -> 메인 구역 추가/숙소 합류 -> 숙소 케어/연습/꾸미기 -> 구역 배치 자동 생산 -> 도감 아이템 업그레이드 -> 입항/비우기 -> 재항해`로 정의한다.
- [x] 기존 "도착 섬에서 전용 자원 획득 후 세관 통과" 루프를 보류/후보 루프로 내린다.
- [x] frontend 첫 화면, 하단 메뉴, 항구, 항해, 숙소가 이 루프를 어떻게 보여줘야 하는지 화면 단위로 적는다.

구현 결과:

- `.cloud/53_core_loop_2026-06-08.md`를 추가해 Phase 1 core loop를 숙소 중심 육성, 항해 발견, 통 인벤토리, 아이동별 도감 아이템, 가변 구역, 배치 자동 생산 중심으로 재정의했다.
- 기존 `destination-island -> ship -> lodge -> customs` 루프는 보류/후보 루프로 내렸다.
- Title/Login/Opening, 메인 화면/마이섬, 항구, 항해, 숙소, 가변 아이동 구역, 생산 구역, 도감/업그레이드, 세관/destination island의 역할을 화면 단위로 정리했다.
- 통 인벤토리, 아이동별 25 도감 아이템, 메인 가변 구역, 숙소 방/마당, 구역 배치/생산, 항해 진행, 배 적재량의 1차 상태 소유 후보를 적었다.
- `.cloud/00_project_overview.md`, `.cloud/40_frontend_rules.md`, `packages/modules/BACKEND_GUIDE.md`에서 새 core loop 문서를 참조하도록 연결했다.
- 검증: 수정 파일 전체에서 한글 깨짐 대체 문자가 없는지 확인했다.

완료 기준:

- 개발자가 다음 화면을 만들 때 어떤 동선을 우선해야 하는지 헷갈리지 않는다.

### 3. 아이동 만남과 가변 메인 구역 설계

- [x] 항해에서 아이동을 만나면 별도 아이동 섬을 소비하는 대신 메인 화면에 해당 아이동 구역 슬롯을 추가하는 모델을 정의한다.
- [x] 만남 순간 오프닝 스토리 1컷, 숙소 합류, 메인 구역 개방 상태 전이를 정의한다.
- [x] 초기 등장 풀은 20~30마리 랜덤으로 둔다.
- [x] 기존 고정 15구역 기획과 5 기본 + 가변 구역 기획의 충돌을 P0 미결로 표시한다.
- [x] 이별하기로 가변 구역을 제거할 때 inventory, 생산, 숙소, 도감 상태를 어떻게 처리할지 후보를 둔다.

구현 결과:

- `.cloud/54_aidong_encounter_dynamic_zone_2026-06-08.md`를 추가해 항해 아이동 만남과 메인 가변 구역 추가 모델을 정의했다.
- 현재 구현의 `my-aidong/recruit`, `my-island/unlock-zone`, `route-neighbor/landing`, `VNPlayer`, `gacha`/`cutscene` 재사용 지점을 정리했다.
- `routeNeighborStates.landings.last`의 `aidong-encounter` 후보와 `myIslandStates.dynamicAidongZones` 후보 schema를 적었다.
- 단기 API 조합안과 권장 `POST /api/modules/route-neighbor/encounter/accept` action API 후보를 비교했다.
- 기존 `/voyage/island/:id/landing`는 "섬 상륙"이 아니라 `/voyage/encounter/:characterId` 계열 만남 화면으로 전환하는 후보로 정리했다.
- 이별하기는 실제 구현하지 않고 `hidden`/`farewelled`, 생산 중단, 도감 인벤토리 비활성화 후보만 남겼다.
- 고정 15구역과 가변 구역 충돌은 P0 미결로 유지했다.
- `.cloud/53_core_loop_2026-06-08.md`와 `packages/modules/BACKEND_GUIDE.md`에 새 설계 문서 링크를 추가했다.
- 검증: 수정 파일 전체에서 한글 깨짐 대체 문자가 없는지 확인했다.

완료 기준:

- `my-island`, `my-aidong`, `route-neighbor`, `gacha/cutscene`이 어떤 상태를 주고받는지 1차 계약이 생긴다.

### 4. 통 인벤토리와 아이동 귀속 도감 인벤토리 재설계

- [x] 슬롯 고정 인벤토리와 세관 강제 변환 모델을 Phase 1에서 내린다.
- [x] "통 인벤토리"의 소유권을 `hostStates.inventory` 중심으로 재정의한다.
- [x] 아이동별 25개 도감 아이템 인벤토리를 `myAidongStates` 또는 별도 전용 collection 후보로 설계한다.
- [x] 항해 공통 아이템과 아이동 귀속 도감 아이템의 경계를 정의한다.
- [x] 현재 `shipInventory`, `lodgeInventory`, `localResources`, `localInventory`의 유지/축소/호환 방향을 정리한다.
- [x] 기존 customs rule 중 Phase 1에서 숨길 rule과 내부 검증용으로 유지할 rule을 분류한다.

구현 결과:

- `.cloud/55_inventory_codex_item_redesign_2026-06-08.md`를 추가해 통 인벤토리와 아이동 귀속 도감 인벤토리 경계를 정의했다.
- 통 인벤토리 권위는 `hostStates.inventory`로 정리했다.
- Aidong 착용 아이템 소유권은 `hostStates.inventory`, 착용 상태는 `myAidongStates.equippedItems` 유지로 정리했다.
- 아이동별 25 도감 아이템은 `myAidongStates.aidongCodexItems`, 업그레이드 상태는 `myAidongStates.aidongUpgradeState` 후보로 정리했다.
- `codexStates`는 아이템 수량 원장이 아니라 도감 해금/등록/표시 진척을 담당하는 것으로 분리했다.
- `shipInventory`, `lodgeInventory`, `zone/localResources`, `destination/localResources`, `route/localResources`는 삭제하지 않고 호환/보류 필드로 낮췄다.
- 새 item scope 후보로 `host-global`, `aidong-codex`, `aidong-equipment`, `voyage-cargo`, `module-local-legacy`를 기록했다.
- `.cloud/53_core_loop_2026-06-08.md`, `packages/modules/ITEM_CATALOG_GUIDE.md`, `packages/modules/BACKEND_GUIDE.md`에 새 설계 문서 링크를 추가했다.
- 검증: 수정 파일 전체에서 한글 깨짐 대체 문자가 없는지 확인했다.

완료 기준:

- "배 인벤토리와 내 인벤토리 구분 없음"이라는 새 기획과 현재 `shipInventory` 구현의 충돌 해소 방향이 문서화된다.

### 5. Customs De-scope 작업

- [x] 세관을 유저 필수 gate가 아니라 내부 infra/Post 후보로 격하한다.
- [x] `CustomsTransferDialog`를 당장 삭제하지 않고 숨김 또는 dev/검증 경로로 낮출지 결정한다.
- [x] destination island 이탈 gate에서 세관 강제 조건을 제거하거나 feature flag로 묶는 작업 후보를 만든다.
- [x] `customsLogs`, idempotency, resource adapter는 기술 자산으로 보존한다.
- [x] 외부 IP 콜라보/연말 리팩토링 시 재개 조건을 문서에 남긴다.

구현 결과:

- `.cloud/56_customs_descope_2026-06-08.md`를 추가해 세관의 Phase 1 운영 정책을 별도 문서로 분리했다.
- 기본 방향을 `customs backend ON, customs user gate OFF`로 정의했다.
- `/api/customs/rules`, `/api/customs/apply`, `/api/customs/logs`, `customsLogs`, resource adapter, `customs.csv` loader, registered rule은 기술 자산으로 유지한다고 정리했다.
- `CustomsTransferDialog`, MiniGame 닫기 세관 gate, destination island 이탈 세관 gate는 삭제가 아니라 숨김/dev/검증 경로 또는 feature flag 후보로 낮췄다.
- frontend feature flag 후보로 `VITE_CUSTOMS_UI_ENABLED`, `VITE_CUSTOMS_EXIT_GATE_ENABLED`, `VITE_DESTINATION_ISLAND_CUSTOMS_GATE_ENABLED`를 기록했다.
- 외부 IP 콜라보, 완전 모듈화, module-local economy, 거래/교환/audit, transaction 운영 확정 시 세관을 재개할 수 있다고 정리했다.

완료 기준:

- 신규 플레이어가 세관 개념을 몰라도 core loop가 돌아가도록 전환 방향이 잡힌다.

### 6. 숙소 중심 육성 허브 재설계

- [x] 숙소를 "방=육성 제한, 마당=보유 아이동 표시, 전체 컨트롤 허브"로 재정의한다.
- [x] 방 수가 적극 육성 가능한 Aidong 수를 제한하도록 상태 모델을 잡는다.
- [x] 숙소에서 케어, 연습, 꾸미기, 업그레이드, 아이동 목록 관리를 연결한다.
- [x] 항해 중/구역 배치 중인 Aidong의 방 꾸미기, 케어, 연습 가능 범위를 다시 정한다.
- [x] 기존 `lodgeInventory`와 방 꾸미기 기능이 새 기획에서 어떤 위치인지 정리한다.

구현 결과:

- `.cloud/57_lodge_growth_hub_2026-06-08.md`를 추가해 숙소를 Phase 1 핵심 육성 허브로 재정의했다.
- `lodgeStates.assignedAidongs`와 `max_assigned_aidongs=5`를 방 수 제한의 현재 구현으로 정리했다.
- `lodgeStates.rooms`와 `lodgeStates.furniture`는 방 꾸미기와 가구 구매 원장으로 유지하고, 빈 방도 꾸밀 수 있어야 한다고 명시했다.
- `lodgeInventory`는 통 인벤토리 전환 전까지 호환/보류 필드로 낮췄다.
- Aidong 활동 상태를 방, 마당, 항해 중, 구역 배치 중, 선실 배치 중, 비활성으로 나누고 케어/연습/꾸미기 허용 범위를 표로 정리했다.
- 숙소 화면은 케어, 밥주기, 연습, 꾸미기, 아이템 착용, 도감 아이템/업그레이드, 방 배정, 구역 배치 후보 확인의 허브로 정의했다.

완료 기준:

- 숙소 화면이 항구/세관보다 먼저 고도화해야 할 핵심 화면으로 재배치된다.

### 7. 구역 배치 자동 생산 설계

- [x] 구역에 Aidong을 배치하면 시간당 자원을 생산하는 모델을 정의한다.
- [x] Aidong 속성/케미/효율에 따라 생산량이 달라지는 rule 후보를 만든다.
- [x] 비효율 배치 표시, 생산 예상량, 회수 버튼의 UI 기준을 잡는다.
- [x] 기존 zone collect/clear API를 자동 생산 모델로 확장할지, 새 placement/production API를 만들지 결정한다.
- [x] 생산 자원은 통 인벤토리로 바로 들어가는지, 구역 임시 저장소를 둘지 결정한다.

구현 결과:

- `.cloud/58_zone_placement_production_2026-06-08.md`를 추가해 구역 배치 자동 생산 설계를 정의했다.
- 생산 상태는 `zoneStates.progress.production` 후보로 두고, `localResources`는 legacy/호환 필드로 낮췄다.
- 기존 `collect/clear`는 미니게임/POC 호환 경로로 유지하고, 자동 생산은 `production/assign`, `production/unassign`, `production/claim` 계열 action 후보로 분리했다.
- 생산 보상은 기본적으로 `hostStates.inventory`에 직접 지급하는 방향으로 정리했다.
- Aidong 영입 여부, 항해 중 여부, 다른 구역 중복 배치, zone unlock, slot limit을 backend service가 검증해야 한다고 정리했다.
- UI는 배치 슬롯, 시간당 예상 생산량, 누적 생산량, 회수 버튼, 효율 badge, 비효율 경고를 보여주는 방향으로 잡았다.

완료 기준:

- "부려먹는 아이동" 트랙이 실제 backend state와 화면 작업으로 쪼개진다.

### 8. 아이동별 미니게임 스킨 모델

- [x] 4종 고정 구역 미니게임 모델을 아이동별 지정게임 + 스킨 모델로 재정리한다.
- [x] 같은 미니게임 엔진을 여러 Aidong이 공유하고, 이미지/획득 아이템/문구만 다르게 쓰는 구조를 정의한다.
- [x] `zone-garden`, `zone-oasis`, `zone-memory`, `zone-mine`을 그대로 둘지, minigame engine으로 재분류할지 정한다.
- [x] 미니게임 입장재와 보상 아이템의 출처를 정리한다.
- [x] 초기 20~30마리 기준으로 어떤 Aidong이 어떤 game skin을 쓰는지 catalog 후보를 만든다.

구현 결과:

- `.cloud/59_aidong_minigame_skin_model_2026-06-08.md`를 추가해 공통 game engine + Aidong별 skin 모델을 정의했다.
- `zone-garden`, `zone-oasis`, `zone-memory`, `zone-mine`은 당장 삭제하거나 이름 변경하지 않고 `garden-grow`, `idle-rest`, `memory-match`, `mine-dig` engine 후보로 재분류했다.
- skin catalog 후보로 `packages/modules/my-aidong/minigame-skins.csv`를 제안하고, `characterId`, `skinId`, `engineId`, `entryItemId`, `rewardItemId`, asset id 컬럼 후보를 적었다.
- 미니게임 결과 검증은 engine별 backend validation이 담당하고, frontend는 result event만 보내는 원칙을 유지했다.
- 일반 보상은 `hostStates.inventory`, Aidong별 도감 아이템 보상은 `myAidongStates.aidongCodexItems`로 들어가는 방향을 정리했다.
- 입장재는 P1 미결로 두되, 소유권은 `hostStates.inventory` 또는 Aidong 귀속 inventory 중 하나로 확정해야 한다고 정리했다.

완료 기준:

- 766마리 개별 게임을 만들지 않는 재사용 구조가 코드/데이터 기준으로 표현된다.

### 9. 도감 아이템과 업그레이드 경제 설계

- [x] 아이동별 25 도감 아이템 세트 스키마를 만든다.
- [x] 도감 아이템을 모으는 것에서 끝내지 않고 업그레이드 연료로 소비하는 흐름을 정의한다.
- [x] 댄스머신, 랩머신 같은 업그레이드 종류와 요구 아이템 후보를 적는다.
- [x] 업그레이드 결과가 능력치, 생산 효율, 미니게임, 외형 중 어디에 영향을 주는지 분류한다.
- [x] 친구 간 교환은 Post-Proto 후보로 보류한다.

구현 결과:

- `.cloud/60_codex_upgrade_economy_2026-06-08.md`를 추가해 아이동별 25개 도감 아이템과 숙소 업그레이드 경제를 정의했다.
- 도감 아이템 수량은 `myAidongStates.aidongCodexItems`, 업그레이드 상태는 `myAidongStates.aidongUpgradeState`, 연습 상태는 `myAidongStates.practiceState` 후보로 정리했다.
- `codexStates`는 아이템 수량 원장이 아니라 도감 표시, 등록, 일지 진행 상태만 담당하는 것으로 경계를 명시했다.
- `packages/modules/my-aidong/codex-items.csv`, `packages/modules/my-aidong/upgrade-recipes.csv` 후보와 컬럼 초안을 작성했다.
- 댄스머신, 랩머신, 보컬부스, 스타일링 데스크 같은 업그레이드 후보와 생산 효율, 미니게임 보상, 케어 효율, 외형, 스토리/도감 해금 효과 후보를 정리했다.
- 친구 간 교환과 766개 전체 도감 구현은 Post-Proto/장기 확장으로 보류했다.

완료 기준:

- `codex`가 단순 수집 도감에서 성장 재료 시스템으로 확장되는 방향이 잡힌다.

### 10. 항해 보드 단순화 결정 준비

- [x] 주사위 채택 여부를 P0 미결로 유지하고, 재논의용 선택지를 정리한다.
- [x] 현재 30칸/주사위 모델, 8~9칸 축소 모델, 지정 이동 모델의 장단점을 비교한다.
- [x] 첫 사용자 난이도와 항해 피로도를 기준으로 UX 검토표를 만든다.
- [x] 코드에서는 현재 보드를 확정 밸런스로 더 고도화하지 않는다.
- [x] 필요하면 feature flag로 보드 메카닉을 교체 가능한지 조사한다.

구현 결과:

- `.cloud/61_voyage_board_simplification_2026-06-08.md`를 추가해 항해 보드 단순화 결정 자료를 작성했다.
- 30칸 주사위 유지, 8~9칸 축소 보드, 지정 이동, 하이브리드 모델의 장단점과 추천도를 비교했다.
- 첫 사용자 이해, 반복 피로도, 아이동 만남 템포, 파밍 방식, 현재 코드 재사용성, 화면 설명 비용, 장기 확장성 기준의 UX 검토표를 만들었다.
- 임시 권장안은 8~9칸 축소 보드로 두되, 주사위 채택 여부와 최종 이동 방식은 P0 미결로 유지했다.
- `VITE_VOYAGE_BOARD_MODE`, `VOYAGE_BOARD_MODE` feature flag/config 후보를 기록했다.
- 결정 전 30칸 보드 전용 밸런스, asset, tutorial, 보상 테이블 고도화는 금지선으로 정리했다.

완료 기준:

- 월요일 재논의에서 주사위/칸 수/이동 방식 결정을 바로 내릴 수 있는 자료가 생긴다.

### 11. 배 적재량과 입항 사이클 재정의

- [x] 배 적재량은 "입항해 비우고 재출항" 사이클의 압력 장치로 둔다.
- [x] 배 업그레이드가 적재량과 선실 확장에 주는 효과를 정리한다.
- [x] 항해 공통 아이템은 배 화물칸에 적재하고, 아이동 도감 아이템은 Aidong 귀속으로 분리한다.
- [x] 현재 `shipInventory`, `cargo`, `cargoCapacity`의 용어를 새 기획에 맞춰 정리한다.
- [x] 항구에서 비우기/판매/보관/제작으로 이어지는 후보 흐름을 정리한다.

구현 결과:

- `.cloud/62_ship_cargo_port_cycle_2026-06-08.md`를 추가해 배 적재량과 입항/비우기 사이클을 재정의했다.
- `shipInventory`는 영구 인벤토리가 아니라 항해 중 입항 전 임시 화물 또는 적재량 압력 원장으로 정리했다.
- `cargo`는 legacy/예약 필드, `cargoCapacity`는 `shipTypeId`와 `ship/balance.csv`에서 계산되는 값으로 정리했다.
- 항해 공통 화물은 `shipStates.shipInventory`, 아이동 도감 아이템은 `myAidongStates.aidongCodexItems`, 일반 영구 보관은 `hostStates.inventory`로 분리했다.
- 항구 action 후보로 `unload`, `sell-cargo`, `store-cargo`, `craft-from-cargo`, `discard-cargo`를 정리하고, 초기 구현은 `unload` 우선으로 뒀다.
- 배 업그레이드 효과 후보를 적재량 증가, 선실 증가, 갑판 증가, 항해 효율, 입항 보너스로 나눴다.

완료 기준:

- 배 인벤토리/내 인벤토리/아이동 인벤토리 경계가 다시 정의된다.

### 12. 웹 UI/UX 방향 재정렬

- [x] 웹은 좁은 모바일 게임보다 깊은 뎁스와 정보량을 수용하는 방향으로 잡는다.
- [x] 멀티탭/멀티세션을 허용하되 복사 버그만 차단하는 QA 기준을 추가한다.
- [x] 딥링크는 목표가 아니며, 라우트 분리는 유지보수 편의 기준으로 둔다.
- [x] 메인 화면에 가변 구역이 늘어날 때의 네비게이션 방식을 후보화한다.
- [x] UI 디자인 착수 전에 필요한 화면 목록과 우선순위를 만든다.

구현 결과:

- `.cloud/63_web_ui_ux_direction_2026-06-08.md`를 추가해 웹 UI/UX 방향을 재정렬했다.
- 웹 UI를 모바일 게임 축소판이 아니라 아기자기하지만 깊이 있는 웹형 생활 육성 UI로 정의했다.
- 숙소 허브, 메인 가변 구역, 항해 만남/수집, 통 인벤토리, 구역 배치 자동 생산, 항구 입항/비우기 순서로 화면 우선순위를 정리했다.
- 멀티탭/멀티세션은 허용하되 중복 보상, 중복 소비, stale state 덮어쓰기, Aidong 중복 배치 같은 QA 기준을 추가했다.
- 딥링크는 1차 목표가 아니며, route 분리는 화면 책임 분리와 smoke 안정성을 위한 기준으로 정리했다.
- 메인 가변 구역 navigation 후보를 grid, 지도형 배치, category tab, 즐겨찾기 dock으로 비교했다.

완료 기준:

- "아기자기하지만 깊이 있는 웹 UI" 방향이 화면 작업 기준으로 내려온다.

### 13. Backend/API 영향도 조사

- [x] 현재 backend module route 중 새 기획에서 유지할 route, 숨길 route, 바꿀 route를 분류한다.
- [x] `/api/modules/ship`, `/api/modules/lodge`, `/api/modules/route-neighbor`, `/api/modules/my-aidong`, `/api/modules/codex` 영향도를 정리한다.
- [x] 세관 강제 제거가 smoke test에 미치는 영향을 확인한다.
- [x] 통 인벤토리 전환 시 repository/model migration 필요성을 조사한다.
- [x] 기술팀 판단 영역인 통/분리 최종 결정은 문서상 미결로 남긴다.

구현 결과:

- `.cloud/64_backend_api_impact_2026-06-08.md`를 추가해 backend module route, 신규 API 후보, model 영향도, customs/smoke 영향도를 정리했다.
- `codex`, `lodge`, `my-aidong`, `my-island`, `route-neighbor`, `ship`, `zone`, `destination-island` route를 유지/승격/보류/숨김/재정의 대상으로 분류했다.
- `my-aidong`의 도감 아이템/연습/업그레이드 API, `route-neighbor`의 encounter/landing API, `my-island`의 dynamic zone API, `zone`의 production API, `ship`의 unload API 후보를 정리했다.
- `myAidongStates`, `myIslandStates`, `zoneStates`, `shipStates`, `codexStates`의 model 영향도를 정리했다.
- 세관 UI gate 제거가 smoke에 주는 영향을 정리하고, customs backend는 기술 자산으로 유지하되 새 core loop smoke에서는 필수 통과 조건으로 삼지 않는다고 명시했다.
- 통 인벤토리 전환은 대규모 migration 없이 `hostStates.inventory` 승격과 기존 `shipInventory`, `lodgeInventory`, `localResources` 호환 유지로 진행하는 방향을 정리했다.

완료 기준:

- 실제 코드 수정 전에 어떤 API를 건드릴지 범위가 보인다.

### 14. Frontend 화면 영향도 조사

- [x] `HarborScene`, `NavigationBoardScene`, `LodgeScene`, `DestinationIslandScreen`, `IslandFullMapScreen`의 새 기획 충돌을 정리한다.
- [x] 세관 중심 UI를 숨기거나 보류 표시할 화면을 찾는다.
- [x] 숙소 중심 허브와 가변 구역 슬롯을 우선 화면으로 재배치한다.
- [x] 기존 PixiJS/Rive destination island 작업은 Post 후보로 보존한다.
- [x] 하단 메뉴와 메인 진입 흐름을 새 core loop 기준으로 다시 잡는다.

구현 결과:

- `.cloud/65_frontend_screen_impact_2026-06-08.md`를 추가해 주요 frontend 화면 영향도를 정리했다.
- `HarborScene`, `NavigationBoardScene`, `LodgeScene`, `IslandFullMapScreen`, `DestinationIslandScreen`, `BottomNav`, `App routes`를 유지/변경/숨김/보류 대상으로 분류했다.
- 숙소는 Phase 1 핵심 허브로 승격하고, lodge inventory/customs UI는 호환/보류 section으로 낮추는 방향을 정리했다.
- `NavigationBoardScene`은 30칸 보드 고도화를 중지하고 landing을 Aidong encounter/material/codex/event 중심으로 재정의하는 방향을 정리했다.
- `DestinationIslandScreen`의 PixiJS/Rive 작업은 삭제하지 않고 POC/이벤트 섬/Post 후보로 보존한다고 명시했다.
- 세관 중심 UI는 기본 core loop에서 숨기고 dev/검증/feature flag 경로로 유지하는 화면들을 정리했다.
- 화면 작업 순서를 `LodgeScene`, `IslandFullMapScreen`, `NavigationBoardScene`, `HarborScene`, `BottomNav`, `DestinationIslandScreen`, `CustomsTransferDialog` 순으로 제안했다.

완료 기준:

- 화면별 수정 순서가 나온다.

### 15. 검증과 테스트 재정렬

- [x] 기존 `check:live-smoke:local`이 세관 중심 루프에 과도하게 묶여 있는지 확인한다.
- [x] 새 smoke 기준을 "로그인/숙소/항해/아이동 만남/구역 추가/통 인벤토리" 중심으로 재설계한다.
- [x] `audit:module-items`, `audit:module-decor`는 유지하되, customs rule 필수성은 Phase 1 기준에서 완화할지 검토한다.
- [x] 멀티세션 동시 조작 QA 항목을 추가한다.
- [x] 기획 보류 기능은 smoke 실패 원인이 되지 않도록 분리한다.

구현 결과:

- `.cloud/66_validation_test_realignment_2026-06-08.md`를 추가해 검증 기준을 core smoke, compat/POC smoke, audit, multi-session QA로 재정렬했다.
- `check:live-smoke:local`은 local Mongo, backend, frontend를 묶는 넓은 회귀검사로 유지하되, Phase 1 UX 정의 자체로 보지 않는다고 정리했다.
- `check:e2e:smoke`의 로그인, 주요 route, legacy `/api/state` 부재, 세관 버튼 비노출 검증은 core 기준으로 유지하고, `/voyage/island/shell`은 compat/dev route smoke 후보로 분리했다.
- `check:module-actions-smoke`는 backend split 저장소와 action API 검증으로 유지하되, customs/ship/lodge/destination POC 항목은 core/compat 분리 후속 대상으로 정리했다.
- `audit:module-items`, `audit:module-decor`, `audit:module-catalogs`는 유지하지만 모든 item에 customs rule이 필요하다는 해석은 Phase 1에서 완화했다.
- 멀티세션 QA 기준으로 중복 보상, 중복 소비, stale state 덮어쓰기, Aidong 중복 배치, 항해 중 Aidong 조작 제한을 추가했다.

완료 기준:

- 테스트가 이전 기획을 강제하지 않고 새 core loop를 지키게 된다.

### 16. 세관 UI/UX 제거와 Customs 비강제화

- [x] frontend에서 세관 팝업, 세관 버튼, 모듈 이탈 세관 gate가 core route에 노출되는 지점을 다시 조사한다.
- [x] `CustomsTransferDialog`는 삭제보다 dev/compat/feature flag 경로로 격리한다.
- [x] 항구, 숙소, zone, destination island, minigame close 흐름에서 세관이 사용자 동선을 막지 않도록 정리한다.
- [x] backend customs API, customsLogs, resource adapter는 유지한다.
- [x] module route/service/repository/model 경계와 Mongo collection 분리는 유지한다.
- [x] 신규 gameplay 보상은 customs를 통하지 않고 domain action API가 권위 있게 `hostStates.inventory` 또는 Aidong 귀속 원장에 반영하도록 한다.
- [x] customs 관련 smoke는 Phase 1 core smoke가 아니라 compat/dev smoke로 분리한다.
- [x] 문서 전반에서 "세관 삭제"가 아니라 "세관 강제 UI/UX 제거, backend customs 비강제 유지"로 표현을 맞춘다.

구현 결과:

- `MiniGameModal`의 zone 이탈 세관 gate를 `VITE_CUSTOMS_EXIT_GATE_ENABLED=true` 뒤로 격리했다. 기본값에서는 전용 자원이 있어도 세관 팝업 없이 닫힌다.
- `HarborScene`, `LodgeScene`, `NavigationBoardScene`의 세관 버튼과 `CustomsTransferDialog` 렌더를 `VITE_CUSTOMS_UI_ENABLED=true` 뒤로 격리했다.
- `DestinationIslandScreen`의 섬 이탈 세관 gate와 customs rule 로딩을 `VITE_DESTINATION_ISLAND_CUSTOMS_GATE_ENABLED=true` 뒤로 격리했다. 기본값에서는 섬을 떠날 때 local resource 때문에 막히지 않는다.
- `App`의 전역 `CustomsConfirmModal`을 `VITE_CUSTOMS_UI_ENABLED=true` 뒤로 격리했다.
- backend customs API, customs rule, customsLogs, resource adapter, module storage 경계는 건드리지 않았다.
- 숙소와 항해 보드의 인벤토리 안내 문구를 세관 전제에서 통 인벤토리와 전용 action API 후보 중심으로 수정했다.
- 검증: `pnpm --filter frontend typecheck`, `pnpm --filter @idongworld/destination-shell-island typecheck`를 통과했다.

완료 기준:

- 사용자는 Phase 1 core loop에서 세관을 보지 않아도 플레이가 가능하다.
- backend 모듈화와 customs 기술 자산은 남아 있어 외부 IP 콜라보나 후속 리팩토링 때 재사용할 수 있다.
- 모듈화는 유지되지만, 모듈 경계가 사용자에게 불필요한 변환 절차로 노출되지 않는다.

### 17. 기획 확정 전 선행 가능한 내 구역 관리 기반 작업

기획 수치나 Aidong별 상세 데이터가 들어오기 전에도, 항해 중 만난 Aidong의 전용 구역을 메인 화면에 추가하기 위한 공통 관리 기반은 먼저 만들 수 있다.

- [x] `myIslandStates`에 가변 Aidong 구역 목록을 담을 최소 schema 후보를 확정한다.
- [x] `dynamicAidongZones`의 필드를 `zoneId`, `characterId`, `status`, `displayOrder`, `pinned`, `openedAt`, `source` 중심으로 정의한다.
- [x] 기본 구역과 Aidong 가변 구역을 함께 조회하는 selector/facade 후보를 만든다.
- [x] 메인 화면에서 구역 목록을 정렬, 숨김, 고정 표시할 수 있는 UI 껍데기 후보를 만든다.
- [x] 구역 관리 화면 또는 패널의 최소 UX를 정의한다. 예: 표시/숨김, 즐겨찾기, 정렬, 이별하기 placeholder.
- [x] Aidong 위치 중복 방지를 위한 공통 location selector 후보를 만든다.
- [x] 중복 배치 검증 기준을 문서화한다. 예: 숙소 방, 마당, 항해 중, 선실, 갑판, 항구 지원, 구역 생산 배치.
- [x] 실제 생산량, 미니게임 skin, 보상 item은 기획 확정 전까지 placeholder로 둔다.
- [x] 가변 구역 API는 실제 encounter 수락 API가 오기 전까지 `my-island` action 후보로만 설계한다.
- [x] smoke 후보를 작성한다. 예: 같은 Aidong 구역 중복 생성 금지, 숨김 구역 미표시, 고정 구역 우선 표시.

구현 결과:

- `packages/backend/src/models/MyIslandStateModel.ts`에 `dynamicAidongZones` 필드를 추가했다.
- `packages/backend/src/repositories/moduleDefaults.ts`와 `packages/backend/src/modules/modelSpecs.ts`에 my-island 소유 필드와 기본값을 반영했다.
- `packages/backend/src/modules/my-island/service.ts`에 가변 Aidong 구역 열기와 업데이트 action을 추가했다.
- `packages/backend/src/modules/my-island/routes.ts`에 `POST /api/modules/my-island/dynamic-zones/open`, `POST /api/modules/my-island/dynamic-zones/update` route를 추가했다.
- `packages/frontend/src/stores/userStore.ts`, `packages/frontend/src/lib/syncStore.ts`, `packages/frontend/src/lib/storeFacades.ts`, `packages/frontend/src/lib/api.ts`에 dynamic zone 상태, sync, facade, API 연결부를 추가했다.
- `packages/frontend/src/screens/IslandFullMapScreen.tsx`에 `Aidong 전용 구역 관리` placeholder section을 추가했다. 현재는 고정/숨김/다시 표시 조작만 제공하며, 실제 생산량과 보상은 확정 전 placeholder로 둔다.
- `.cloud/68_dynamic_zone_management_shell_2026-06-08.md`를 추가해 저장 구조, backend 계약, frontend 표시 기준, Aidong 위치 중복 방지 후보, smoke 후보를 정리했다.
- 검증: `pnpm --filter backend typecheck`, `pnpm --filter frontend typecheck`, `pnpm check:backend:model-specs`를 통과했다.

완료 기준:

- 실제 Aidong별 기획이 들어왔을 때 구역 목록 저장, 표시, 정렬, 숨김, 중복 방지 기반을 바로 연결할 수 있다.
- 고정 15구역과 가변 Aidong 구역의 최종 기획이 미결이어도, 데이터 구조는 둘을 함께 담을 수 있다.
- 이별하기, 생산 수치, 미니게임 skin, 보상 item은 placeholder로 남기고 확정 기획을 기다린다.

### 18. 기획 확정 전 선행 가능한 Backend/API 껍데기 작업

실제 수치와 콘텐츠가 없어도, 새 core loop를 받을 수 있는 API와 validation shell은 준비할 수 있다.

- [x] `my-island` service에 dynamic zone 추가/숨김/정렬 action 후보를 설계한다.
- [x] `route-neighbor/encounter/accept` 후보 API의 request/response 타입을 문서화한다.
- [x] 실제 구현 전에는 encounter pool과 확률은 placeholder 또는 config 후보로 둔다.
- [x] `zone production` action의 최소 request/response 타입을 정리한다.
- [x] `production/assign`, `production/unassign`, `production/claim`의 validation 책임을 분리한다.
- [x] `my-aidong`의 도감 아이템/업그레이드 API는 실제 25종 item이 오기 전까지 타입과 원장 후보만 준비한다.
- [x] 신규 action은 `hostStates.inventory` 또는 `myAidongStates` 후보 원장에 직접 반영하는 방향으로 설계한다.
- [x] customs를 신규 core action에 끼워 넣지 않는다.

구현 결과:

- `packages/backend/src/models/RouteNeighborStateModel.ts`에 `progress` 필드를 추가하고, `routeNeighborStates.progress.acceptedEncounters`를 Aidong 만남 수락 기록 후보로 잡았다.
- `packages/backend/src/modules/route-neighbor/service.ts`, `routes.ts`에 `POST /api/modules/route-neighbor/encounter/accept`를 추가했다. 이 route는 `my-aidong` 영입과 `my-island` 가변 구역 열기를 orchestration한다.
- `packages/backend/src/models/MyAidongStateModel.ts`에 `aidongCodexItems`, `aidongUpgradeState` 후보 원장을 추가했다.
- `packages/backend/src/modules/my-aidong/service.ts`, `routes.ts`에 `POST /api/modules/my-aidong/codex-items/grant`, `POST /api/modules/my-aidong/upgrades/request`를 추가했다.
- `packages/backend/src/modules/zone/service.ts`, `routes.ts`에 `production/assign`, `production/unassign`, `production/claim` shell을 추가했다. 현재 `claim`은 기록만 남기고 보상은 지급하지 않는다.
- `packages/backend/src/repositories/moduleDefaults.ts`, `packages/backend/src/modules/modelSpecs.ts`에 새 필드 기본값과 소유권을 반영했다.
- `packages/backend/scripts/smoke-module-actions.mjs`에 신규 shell route 호출을 추가했다.
- `.cloud/69_backend_api_shell_2026-06-08.md`를 추가해 request/response 후보, 검증 책임, placeholder 범위, smoke 반영 내용을 정리했다.
- 검증: `pnpm --filter backend typecheck`, `pnpm check:backend:model-specs`, `pnpm check:module-actions-smoke`를 통과했다.

완료 기준:

- 기획 데이터가 들어오기 전에도 route/service/repository/model 경계가 흔들리지 않는다.
- 실제 구현 시 기획 수치만 채워 넣으면 되는 API 단위가 보인다.

### 19. 기획 확정 전 선행 가능한 Frontend 껍데기 작업

실제 art asset과 수치가 없어도, 화면 구조와 상태 연결부는 placeholder로 준비할 수 있다.

- [ ] 마이섬 메인에 기본 구역과 가변 Aidong 구역을 함께 보여줄 layout 후보를 만든다.
- [ ] 구역 카드 component 후보를 만든다. 필드: 이름, Aidong, 상태, 생산 badge, 즐겨찾기, 숨김 여부.
- [ ] 구역 관리 패널 후보를 만든다.
- [ ] 숙소에서 "구역 배치 후보 Aidong"을 보여줄 selector 후보를 만든다.
- [ ] 항해 보드의 character landing 문구를 "섬 상륙"에서 "Aidong 만남"으로 바꿀 준비를 한다.
- [ ] `CustomsTransferDialog`는 core 화면에서 계속 비노출 상태로 유지한다.
- [ ] placeholder UI는 실제 기획 확정 전까지 삭제/확정처럼 보이지 않게 dev label 또는 준비중 상태로 둔다.

완료 기준:

- 실제 기획과 asset이 들어오면 메인 구역 목록, 숙소, 항해 만남 화면에 바로 꽂을 수 있다.
- 아직 확정되지 않은 수치나 경제 규칙을 UI가 확정처럼 보여주지 않는다.

## 우선순위

1. SoT 재정렬과 충돌 지도 작성
2. Core Loop 재정의
3. 통 인벤토리/아이동 도감 인벤토리 경계 재설계
4. 숙소 중심 육성 허브 재설계
5. 아이동 만남과 가변 메인 구역 설계
6. Backend/API 영향도 조사
7. Frontend 화면 영향도 조사
8. 세관 UI/UX 제거와 Customs 비강제화
9. 기획 확정 전 내 구역 관리 기반 작업
10. 기획 확정 전 Backend/API 껍데기 작업
11. 기획 확정 전 Frontend 껍데기 작업

## 보류 항목

- 세관 강제 UX.
- 본격 모듈화 리팩토링.
- 외부 IP 콜라보 전용 아이템.
- 콘서트 예매, 포도알 BM.
- 친구 간 아이템 교환.
- Spine/Live2D 고품질 리깅.
- 766마리 전체 인사이클로피디아.

## 월요일 재논의 질문

- Q1. 항해 주사위를 유지할 것인가, 칸을 줄일 것인가, 지정 이동으로 갈 것인가.
- Q2. 고정 15구역과 가변 아이동 구역을 어떻게 공존시킬 것인가.
- Q3. 배 인벤토리, 통 인벤토리, 아이동 귀속 인벤토리의 정확한 경계는 무엇인가.
- Q4. 세관은 완전히 숨길 것인가, 내부 dev/검증 기능으로 남길 것인가.
- Q5. 초기 20~30마리 랜덤 풀과 본진 5명의 관계를 어떻게 잡을 것인가.
- Q6. 숙소 방 수 제한과 마당 표시 상한을 어떻게 잡을 것인가.
- Q7. 미니게임 입장재와 업그레이드 재료 경제를 어느 정도까지 1차 구현에 넣을 것인가.

## 변경 기록

- **2026-06-08**: 18번 기획 확정 전 Backend/API 껍데기 작업을 완료했다. `route-neighbor/encounter/accept`, zone `production/*`, `my-aidong/codex-items/grant`, `my-aidong/upgrades/request` shell을 추가하고, `RouteNeighborStateModel`, `MyAidongStateModel`, model spec, module action smoke를 확장했다.
- **2026-06-08**: 17번 기획 확정 전 내 구역 관리 기반 작업을 완료했다. `myIslandStates.dynamicAidongZones`, backend open/update action, frontend sync/facade/api, 마이섬 풀맵 관리 placeholder를 추가하고, Aidong 위치 중복 방지 후보와 smoke 기준을 `.cloud/68_dynamic_zone_management_shell_2026-06-08.md`에 정리했다.
- **2026-06-08**: 기획 확정 전 선행 가능한 작업을 17~19번으로 추가했다. 내 구역 관리 기반, dynamicAidongZones schema 후보, Aidong 위치 중복 방지, API shell, frontend placeholder UI를 실제 수치 기획 전에 준비할 수 있는 작업으로 분리했다.
- **2026-06-08**: 실제 기획 입력을 받을 템플릿 `.cloud/67_planning_input_template_2026-06-08.md`를 추가했다. Aidong 만남, 가변 구역, 숙소 육성, 도감 아이템, 업그레이드, 구역 자동 생산, 항해 규칙을 개발 가능한 단위로 받는 양식이다.
- **2026-06-08**: 16번 세관 UI/UX 제거와 Customs 비강제화를 완료했다. `MiniGameModal`, `HarborScene`, `LodgeScene`, `NavigationBoardScene`, `DestinationIslandScreen`, `App`에서 세관 UI와 이탈 gate를 feature flag 뒤로 격리하고, backend customs와 모듈화 경계는 유지했다.
- **2026-06-08**: 세관 관련 후속 작업을 16번으로 추가했다. 기획 변경 가이드 원문에 맞춰 세관 강제 UI/UX는 Phase 1 core loop에서 제거하고, customs backend와 모듈화 경계는 기술 자산으로 유지하는 기준을 명시했다.
- **2026-06-08**: 15번 검증과 테스트 재정렬을 완료했다. `.cloud/66_validation_test_realignment_2026-06-08.md`를 추가하고, `check:live-smoke:local`, `check:e2e:smoke`, `check:module-actions-smoke`, catalog audit을 core/compat/dev 기준으로 재분류했다.
- **2026-06-08**: 14번 Frontend 화면 영향도 조사를 완료했다. `.cloud/65_frontend_screen_impact_2026-06-08.md`를 추가하고, 주요 화면의 유지/변경/숨김 판정과 화면 작업 순서를 정리했다.
- **2026-06-08**: 13번 Backend/API 영향도 조사를 완료했다. `.cloud/64_backend_api_impact_2026-06-08.md`를 추가하고, backend module route의 유지/숨김/변경 판정, 신규 API 후보, model migration 영향, customs/smoke 영향도를 정리했다.
- **2026-06-08**: 12번 웹 UI/UX 방향 재정렬을 완료했다. `.cloud/63_web_ui_ux_direction_2026-06-08.md`를 추가하고, 웹형 생활 육성 UI 방향, 멀티탭 QA 기준, 라우트/딥링크 기준, 메인 가변 구역 navigation 후보를 정리했다.
- **2026-06-08**: 11번 배 적재량과 입항 사이클 재정의를 완료했다. `.cloud/62_ship_cargo_port_cycle_2026-06-08.md`를 추가하고, `shipInventory`를 항해 중 임시 화물/적재량 압력 원장으로, `hostStates.inventory`를 입항 후 영구 보관소로 정리했다.
- **2026-06-08**: 10번 항해 보드 단순화 결정 준비를 완료했다. `.cloud/61_voyage_board_simplification_2026-06-08.md`를 추가하고, 30칸 주사위/8~9칸 축소/지정 이동/하이브리드 선택지를 첫 사용자 난이도와 반복 피로도 기준으로 비교했다.
- **2026-06-08**: 9번 도감 아이템과 업그레이드 경제 설계를 완료했다. `.cloud/60_codex_upgrade_economy_2026-06-08.md`를 추가하고, 아이동별 25 도감 아이템 수량과 숙소 연습/업그레이드 상태를 `myAidongStates` 후보 경계로 정리했다.
- **2026-06-08**: 8번 아이동별 미니게임 스킨 모델을 완료했다. `.cloud/59_aidong_minigame_skin_model_2026-06-08.md`를 추가하고, 4종 zone 미니게임을 공통 engine 후보로 재분류한 뒤 Aidong별 skin catalog로 확장하는 방향을 정의했다.
- **2026-06-08**: 7번 구역 배치 자동 생산 설계를 완료했다. `.cloud/58_zone_placement_production_2026-06-08.md`를 추가하고, 자동 생산 상태를 `zoneStates.progress.production` 후보와 `production/*` action 후보로 분리했다.
- **2026-06-08**: 6번 숙소 중심 육성 허브 재설계를 완료했다. `.cloud/57_lodge_growth_hub_2026-06-08.md`를 추가하고, 숙소를 방=적극 육성 슬롯, 마당=보유/대기 Aidong 표시, 케어·연습·꾸미기·업그레이드 허브로 재정의했다.
- **2026-06-08**: 5번 Customs De-scope 작업을 완료했다. `.cloud/56_customs_descope_2026-06-08.md`를 추가하고, 세관은 backend 기술 자산으로 유지하되 Phase 1 사용자 필수 gate에서는 내리는 정책을 정의했다.
- **2026-06-08**: 4번 통 인벤토리와 아이동 귀속 도감 인벤토리 재설계를 완료했다. `.cloud/55_inventory_codex_item_redesign_2026-06-08.md`를 추가하고, `hostStates.inventory`를 통 인벤토리 권위로, `myAidongStates.aidongCodexItems`를 아이동별 25 도감 아이템 후보 원장으로 정의했다.
- **2026-06-08**: 3번 아이동 만남과 가변 메인 구역 설계를 완료했다. `.cloud/54_aidong_encounter_dynamic_zone_2026-06-08.md`를 추가하고, 항해 만남을 숙소 합류와 메인 가변 구역 슬롯 추가로 이어지는 상태 전이로 정의했다.
- **2026-06-08**: 2번 Core Loop 재정의를 완료했다. `.cloud/53_core_loop_2026-06-08.md`를 추가하고, Phase 1 루프를 항해 발견, 숙소 중심 육성, 통 인벤토리, 아이동별 도감 아이템, 가변 구역, 배치 자동 생산 중심으로 재정리했다.
- **2026-06-08**: 1번 SoT 재정렬과 충돌 지도 작성을 완료했다. `.cloud/52_plan_change_conflict_map_2026-06-08.md`를 추가하고, 주요 `.cloud` 규칙 문서와 `packages/modules` 가이드에 세관/모듈화 강제 UX를 Phase 1에서 보류한다는 기준을 반영했다.
- **2026-06-08**: 2026-06-05 기획 변경 회의 가이드를 기준으로 새 next_work를 작성했다. 이전 `.cloud/49_next_work_2026-06-05_2.md`는 취소 처리하고, 세관/모듈화 강제 UX를 보류하며 숙소 중심 육성, 통 인벤토리, 아이동별 도감 인벤토리, 가변 메인 구역, 배치 자동 생산 중심으로 작업 순서를 재편했다.

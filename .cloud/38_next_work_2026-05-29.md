# 다음 작업 메모 2026-05-29

> 2026-06-12 기준 이 문서는 과거 작업 이력으로 보존한다. 최신 실행 보드는 `.cloud/72_next_work_2026-06-12_week1_a.md`를 따른다.

이 문서는 V1 루트 문서 현행화 이후, 다음에 진행할 작업을 atomic하게 정리한다.

상태: 완료. 2026-05-29 next_work의 1~8번은 모두 처리됐으며, 이 문서는 완료 기록으로만 유지한다.

## 현재 기준

- `.cloud/36_next_work_2026-05-24.md`의 1~8번은 모두 완료됐다.
- 05-24 next_work는 완료 기록으로 폐기하고 새 작업 보드로 사용하지 않는다.
- V1 루트 문서 현행화는 2026-05-29에 1차 완료됐다.
- backend는 단일 Express 서버를 유지하고, 저장소/API 소유권은 module/repository/model/service 경계로 분리한다.
- `/api/state`는 제거 완료 상태다.
- frontend는 단일 Zustand `userStore`를 유지하되 `storeFacades.ts`를 우선 사용한다.
- MongoDB는 local-first다. Atlas는 현재 사용하지 않고, 추후 사용할 수도 있는 선택지로 보류한다.

## 완료된 준비 작업

1. V1 루트 문서 현행화 - 완료
   - 대상: `README_260511검수.md`, `개발계획_260511검수.md`, `데이터연결_다이어그램_260511검수.md`, `Auth_정책_초안_v0.1_260510_260511검수.md`, `PM_결정사항_v0.1_260510_260511검수.md`
   - `/api/state` 호환 경로 표현을 제거 완료 상태로 정리했다.
   - frontend facade 우선 접근과 module action API 흐름을 반영했다.
   - Atlas는 전환 완료가 아니라 보류 상태로 정리했다.
   - 문서별 변경 기록을 한글로 남겼다.

## 완료된 작업 순서

1. shared-data cutscene catalog 검증 도구 추가 - 완료
   - 목표: `shared-data/cutscene-catalog/scenarios.csv`, `diaries.csv`가 실제 시나리오/일기 파일과 맞는지 검증한다.
   - 추가 후보:
     - `tools/audit/checkScenarioCatalog.*`
     - `tools/audit/checkDiaryCatalog.*`
     - root `package.json` script
   - 검증 기준:
     - `written` 상태의 scenario/diary는 실제 파일이 있어야 한다.
     - `planned`, `in-review` 상태는 파일이 없어도 된다.
     - characterId, personaNumber, day 범위 같은 기본 필드를 검사한다.
   - 완료 조건:
     - 실행 가능한 pnpm 명령이 생긴다.
     - 실패 케이스가 사람이 읽을 수 있는 한글 메시지로 나온다.
     - 관련 README와 변경 기록을 갱신한다.
   - 완료 2026-05-29:
     - `tools/audit/checkScenarioCatalog.py`, `tools/audit/checkDiaryCatalog.py`를 추가했다.
     - `tools/audit/checkScenarioCatalog.sh`, `tools/audit/checkDiaryCatalog.sh` wrapper를 추가했다.
     - root `package.json`에 `pnpm audit:scenario-catalog`, `pnpm audit:diary-catalog`, `pnpm audit:cutscene-catalog` 명령을 추가했다.
     - 기본 모드는 CSV 구조와 catalog 정합을 검사하고, 실제 시나리오 루트가 없으면 경고 후 통과한다.
     - `SCENARIO_ROOT`와 `--require-files`를 쓰면 written 파일 존재 검증을 강제한다.

2. aidong-master CSV -> JSON 동기화 도구 추가 - 완료
   - 목표: `shared-data/aidong-master/characters.csv`를 기준으로 `characters.json`을 재생성한다.
   - 현재 문제:
     - CSV와 JSON이 모두 존재하지만 동기화가 수동이다.
   - 추가 후보:
     - `tools/shared-data/syncAidongMaster.*`
     - `pnpm shared-data:sync-aidong`
     - `pnpm shared-data:check-aidong`
   - 완료 조건:
     - CSV를 수정하면 JSON을 안정적으로 재생성할 수 있다.
     - JSON 변경 전후를 확인할 수 있다.
     - `shared-data/aidong-master/README_260511검수.md` 변경 절차를 갱신한다.
   - 완료 2026-05-29:
     - `tools/shared-data/syncAidongMaster.py`, `tools/shared-data/syncAidongMaster.sh`를 추가했다.
     - root `package.json`에 `pnpm shared-data:sync-aidong`, `pnpm shared-data:check-aidong` 명령을 추가했다.
     - `characters.csv`를 기준으로 `characters.json`을 재생성했다.
     - `pnpm shared-data:check-aidong`으로 CSV/JSON 정합을 확인했다.

3. 데이터 파이프라인 최소 검증 묶음 만들기 - 완료
   - 목표: i18n, persona leak, cutscene catalog, aidong-master sync/check를 한 번에 돌릴 수 있는 최소 명령을 만든다.
   - 후보 명령:
     - `pnpm check:data`
   - 포함 후보:
     - `pnpm i18n:check`
     - `pnpm audit:persona`
     - cutscene catalog check
     - aidong-master check
   - 완료 조건:
     - 데이터 변경 후 어떤 명령을 돌려야 하는지 문서에 남긴다.
     - 실패 원인이 파일/행 단위로 보인다.
   - 완료 2026-05-29:
     - root `package.json`에 `pnpm check:data` 명령을 추가했다.
     - `check:data`는 `i18n:check`, `audit:persona --silent`, `audit:cutscene-catalog`, `shared-data:check-aidong`을 순서대로 실행한다.
     - Windows 환경에서 실행되도록 i18n script를 `python` 런처 기준으로 정리했다.
     - `tools/audit/checkPersonaLeak.py`를 추가해 bash/WSL 없이 persona leak 검사를 실행할 수 있게 했다.
     - dev catalog 코드에 남아 있던 영감원 실명 표시를 제거했다.
     - `shared-data/README_260511검수.md`에 최소 검증 명령을 기록했다.

4. zone reward 숫자와 allowlist `balance.csv` 분리 - 완료
   - 목표: zone backend service에 남아 있던 보상 숫자와 zone별 resource/clearId allowlist를 모듈별 `balance.csv`에서 읽도록 1차 분리한다.
   - 확정 원칙:
     - backend service가 권위다.
     - 숫자 보상과 zone별 resource/clearId allowlist는 `packages/modules/zone-*/balance.csv`에서 가져온다.
     - unlock 조건, result payload validation, 지급 가능 여부 검증, idempotency 처리는 backend service 코드에 유지한다.
   - 완료 2026-05-29:
     - `packages/backend/src/modules/zone/balance.ts`를 추가해 zone balance CSV를 backend rule로 로드한다.
     - `packages/backend/src/modules/zone/service.ts`의 collect/clear allowlist와 reward 계산을 CSV 기반 rule로 연결했다.
     - `zone-garden`, `zone-oasis`, `zone-memory`, `zone-mine`의 `balance.csv`에 `zone_collect_resources`, `zone_clear_ids`, reward 계산 값을 추가했다.
     - `pnpm --filter backend typecheck`와 `pnpm test packages/backend/src/backendPersistence.test.ts`를 통과했다.

5. route/ship 기획 확정 전 임시 QA - 완료
   - 목표: cargo, cabin, customs rule을 확정하지 않고도 현재 route/ship 임시 경계가 깨지지 않는지 확인한다.
   - 확인 대상:
     - route-neighbor start/roll/end
     - ship harbor assign/charge
     - routeNeighborStates와 shipStates의 owned field
   - 완료 조건:
     - 현재 기획 미확정 항목과 구현된 항목을 분리해서 문서화한다.
     - 필요한 회귀 테스트가 있으면 backend persistence test나 smoke에 추가한다.
   - 완료 2026-05-29:
     - `routeNeighborStates`는 `currentRoute`, `boardPosition`, `localResources`, `landings`를 소유하는 임시 계약으로 유지한다.
     - `shipStates`는 `harborAssignedChars`, `harborLastChargedAt`, `shipInventory`, `cabins`를 소유하는 임시 계약으로 유지한다.
     - cargo, cabin 효과, route/ship customs rule과 ratio는 아직 확정하지 않는다.
     - `backendPersistence.test.ts`에 ship owned field 계약 확인을 보강했다.
     - `check:module-actions-smoke`에 route-neighbor end 후 owned field와 ship harbor/예약 필드 확인을 보강했다.

6. 실제 화면 기준 수동 QA - 완료
   - 목표: API split 이후 실제 화면 흐름이 이어지는지 확인한다.
   - 최소 흐름:
     - guest login
     - onboarding/title/opening
     - first gacha/recruit
     - island 진입
     - zone clear
     - codex unlock
     - route/ship action
   - 완료 조건:
     - 수동 QA 결과를 runbook 또는 별도 QA 메모에 기록한다.
     - 실패 지점은 재현 절차와 함께 다음 작업으로 분리한다.
   - 완료 2026-05-29:
     - `pnpm check:state-route-runtime:local`을 실행해 local Mongo, backend, frontend runtime gate를 통과했다.
     - frontend root, backend health, Mongo repository, dedicated module, `/api/state` 제거 상태를 확인했다.
     - 오래된 smoke 데이터가 현재 Codex allowlist와 zone clearId allowlist에 맞지 않아 `smoke-persistence.mjs`, `smoke-state-route-removed.mjs`를 수정했다.
     - 수동 클릭 QA 체크리스트와 실행 결과는 `.cloud/39_screen_qa_2026-05-29.md`에 기록했다.

7. Firebase/외부 로그인 운영 결정 - 완료
   - 목표: 실 Firebase project, Google login, Twitter login 도입 시점을 결정하고 skeleton을 준비한다.
   - 확정:
     - 개발 기본 경로는 guest와 local fallback이다.
     - Google/Twitter 버튼은 화면에 보이되 기본값은 준비중이다.
     - 실 key와 secret은 문서에 남기지 않는다.
     - 실제 활성화는 Firebase Console에서 Google/Twitter provider와 redirect domain을 설정한 뒤 `VITE_SOCIAL_LOGIN_ENABLED=true`로 켠다.
   - 완료 2026-05-29:
     - frontend Firebase helper에 Google/Twitter popup skeleton을 준비했다.
     - LoginScreen은 기본적으로 준비중 메시지를 보여주고, flag 활성 시 Firebase popup과 backend session sync를 호출한다.
     - backend `/api/auth/session`을 추가해 Google/Twitter/Firebase uid를 `users` document로 upsert한다.
     - user repository와 UserModel에 `authProvider`, `email`, `displayName`, `photoURL` 필드를 추가했다.
     - auth skeleton 회귀 테스트를 backend persistence test에 추가했다.

8. Atlas 사용 여부 결정 후 readiness - 완료 2026-05-29
   - 목표: Atlas를 당장 사용하지 않고, 추후 사용할 수도 있는 선택지로만 남긴다.
   - 확정:
     - 현재 개발과 검증은 local MongoDB 기준으로 진행한다.
     - Atlas는 도입 확정 사항이 아니라 future option이다.
     - 실제 Atlas URI, database user, network access가 준비되기 전에는 readiness를 실행하지 않는다.
     - Atlas URI와 credential은 문서에 남기지 않는다.
   - 보류 조건:
     - Atlas 사용이 확정되고 실제 credential이 준비된 뒤에만 `pnpm check:mongo:atlas`를 실행한다.
     - transaction 사용 여부는 그때 `transactionsSupported=true` 확인 뒤 결정한다.

## 진행 방식

- 위 순서대로 하나씩 진행한다.
- 한 번호를 시작하면 구현, 검증, 문서 변경 기록까지 같은 단위로 닫는다.
- 기획 결정이 필요한 항목은 코드보다 문서 결정 기록을 먼저 남긴다.
- `.cloud`와 루트 V1 문서의 변경 기록은 한글로 작성한다.
- 한글 문서를 수정한 뒤에는 깨진 문자와 오래된 `/api/state` 표현을 검색한다.

## 다음 시작 지점

38번 next_work의 1~8번은 완료했다. 다음 작업은 새 next_work 문서에서 다시 번호를 매긴다.

## 변경 기록

- **2026-05-29**: 05-24 next_work 완료 여부를 확인하고 폐기했다. V1 루트 문서 현행화 이후의 새 작업 보드를 shared-data 검증, aidong-master 동기화, 데이터 검증 묶음, zone config 검토, route/ship QA, 화면 QA, Firebase/Atlas 결정 순서로 작성했다.
- **2026-05-29**: 1번 shared-data cutscene catalog 검증 도구 추가를 완료했다. `pnpm audit:cutscene-catalog` 명령으로 scenarios/diaries CSV 구조와 기본 정합을 확인하고, `SCENARIO_ROOT`와 `--require-files`로 written 파일 존재 검증을 강제할 수 있게 했다.
- **2026-05-29**: 2번 aidong-master CSV -> JSON 동기화 도구 추가를 완료했다. `pnpm shared-data:sync-aidong`으로 `characters.json`을 재생성하고, `pnpm shared-data:check-aidong`으로 정합을 확인할 수 있게 했다.
- **2026-05-29**: 3번 데이터 파이프라인 최소 검증 묶음 만들기를 완료했다. `pnpm check:data`를 추가하고, Windows에서도 실행되도록 i18n/persona 검증 명령을 정리했다. dev catalog 코드에 남아 있던 영감원 실명 표시도 제거했다.
- **2026-05-29**: 4번 zone reward 숫자와 allowlist 분리를 완료했다. zone balance CSV에서 collect resource allowlist, clearId allowlist, reward 숫자를 읽는 backend loader를 추가했고, validation/unlock/idempotency는 backend service 코드에 유지했다.
- **2026-05-29**: 5번 route/ship 기획 확정 전 임시 QA를 완료했다. route/ship의 현재 owned field 계약을 문서에 고정하고, backend persistence test와 `check:module-actions-smoke`에 route-neighbor/ship 회귀 확인을 보강했다.
- **2026-05-29**: 6번 실제 화면 기준 수동 QA를 완료했다. `pnpm check:state-route-runtime:local`로 local Mongo/backend/frontend runtime gate를 통과했고, 결과와 남은 클릭 QA 체크리스트를 `.cloud/39_screen_qa_2026-05-29.md`에 기록했다.
- **2026-05-29**: 7번 Firebase/외부 로그인 운영 결정을 완료했다. Google/Twitter 로그인은 기본 UI에서는 준비중으로 유지하되, Firebase provider 설정과 `VITE_SOCIAL_LOGIN_ENABLED=true` 이후 동작할 frontend/backend skeleton을 추가했다.
- **2026-05-29**: 8번 Atlas 판단을 완료했다. Atlas는 현재 사용하지 않고 추후 사용할 수도 있는 선택지로 보류하며, local MongoDB 우선 개발과 credential 문서화 금지 기준을 유지한다.
- **2026-05-29**: 05-29 next_work 전체 완료 확인을 마쳤다. 1~8번은 모두 완료 상태이며, 이 문서는 다음 작업 보드가 아니라 완료 기록으로만 유지한다.

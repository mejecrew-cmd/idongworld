# 다음 작업 메모 2026-06-05 2차

> 2026-06-12 기준 이 문서는 과거 작업 이력으로 보존한다. 최신 실행 보드는 `.cloud/72_next_work_2026-06-12_week1_a.md`를 따른다.

> **상태: 기획 변경으로 취소됨**
>
> 2026-06-08 기준 `.cloud/기획변경회의에따른기획변경가이드.md`가 상위 입력으로 들어오면서 이 작업 보드는 더 이상 다음 구현 순서로 사용하지 않는다.
> 1~11번까지 완료된 작업은 구현 이력으로 보존하되, 남은 12번과 이후 우선순위는 폐기한다.
> 후속 작업은 `.cloud/51_next_work_2026-06-08.md`를 기준으로 진행한다.

이 문서는 `.cloud/43_next_work_2026-06-03.md`의 1~13번 작업을 마친 뒤 이어서 진행할 작업을 정리한다.

현재까지 destination island skeleton, PixiJS/Rive 부분 도입, signed URL backend skeleton, customs 확장, route landing reward 이전, ship inventory/cargo 정식화, lodge/ship decor schema 정리, 자동 검증 확장을 완료했다. 이후 작업은 실제 화면 검증과 화면 완성도를 중심으로 진행한다.

## 현재 완료 기준

- `destination-shell-island`는 backend 전용 document, action API, frontend route, Pixi scene, Rive reaction layer를 가진다.
- route resource landing은 `destination-shell-island` 진입과 `shell-fragment` 보상으로 연결되어 있다.
- `deck-cargo`는 기존 smoke/임시 흐름 호환 보상으로만 유지한다.
- `shipInventory`는 배 적재 자원의 권위 저장소다.
- `cargoCapacity`는 `ship/balance.csv`의 `shipTypeId`에서 계산한다.
- `cargo`는 legacy/reserved 필드다.
- 숙소/선실 꾸미기 catalog는 `decor.csv`로 분리되어 있다.
- 선실 가구 구매 수량은 `shipStates.cabinFurniture`에 저장한다.
- `shell-fragment`는 `destination-shell-island`, `ship`, `lodge` item catalog와 customs rule에 반영되어 있다.
- `decorCatalog.ts`는 모듈 decor 경로의 실제 위치인 `packages/modules/{moduleId}/decor.csv`를 바라본다.

## 작업 순서

### 1. Live Smoke 검증

- [x] local Mongo, backend, frontend를 실제로 띄워 검증한다.
- [x] `pnpm check:module-actions-smoke`를 실행한다.
- [x] `pnpm check:e2e:smoke`를 실행한다.
- [x] `pnpm check:state-route-runtime:local`을 실행한다.
- [x] 위 검증을 한 번에 실행하는 `pnpm check:live-smoke:local` 명령을 추가한다.
- [x] `/api/modules/ship/config`, `/api/modules/destination-shell-island/config`, `/api/customs/rules`가 live smoke 흐름에서 검증되는지 확인한다.

검증 결과:

- `pnpm check:state-route-runtime:local`: 통과.
- `pnpm check:live-smoke:local`: 통과.
- `check:live-smoke:local` 안에서 module action smoke, Playwright e2e smoke, state-route runtime smoke가 모두 통과했다.
- Mongo는 `mongodb://127.0.0.1:27017/idongworld` 로컬 연결로 검증했다.

### 2. Destination Island 이탈 세관 Gate 연결

- [x] 도착 섬 화면에서 섬을 떠나기 전에 세관 팝업을 띄운다.
- [x] `localResources`에 남은 `shell-fragment`가 있으면 이탈 전 정리를 유도한다.
- [x] `shell-fragment-to-ship`과 `shell-fragment-to-coins` rule을 보여준다.
- [x] 배로 싣기 rule은 destination island 이탈 gate에서만 직접 호출한다.
- [x] 내 섬 내부 구역에서는 배 적재 rule을 숨기는 기존 세관 UI 조건을 유지한다.

구현 결과:

- `destination-shell-island` 화면의 `항해 보드`, `섬 떠나기` 버튼은 남은 섬 전용 자원이 있을 때 바로 이동하지 않고 세관 dialog를 먼저 연다.
- 세관 dialog는 섬 전용 자원 목록, 변환 가능한 목적지, 1회 변환, 모두 변환, 모두 배에 싣기를 제공한다.
- 남은 전용 자원이 있거나 세관 rule이 없는 자원이 있으면 `항해 보드로 나가기` 버튼을 막는다.
- `shell-fragment-to-ship` rule은 실제 보상 단위에 맞춰 1개 단위로 배 인벤토리에 실을 수 있게 조정했다.
- `shell-fragment-to-coins` rule은 10개 단위 전역 코인 정산으로 유지한다.

### 3. Customs UI UX 마무리

- [x] 세관 팝업 상단에는 source module inventory를 표시한다.
- [x] 아이템 클릭 시 하단에 변환 가능한 target rule을 보여준다.
- [x] 단일 변환 버튼과 모두 변환 버튼을 제공한다.
- [x] 변환 성공 후 source inventory와 target 상태가 즉시 갱신되는지 확인한다.
- [x] rule이 없을 때의 안내 문구를 정리한다.

구현 결과:

- 공용 `CustomsTransferDialog`의 깨진 문구를 정상 한글로 복구했다.
- 위쪽은 source module inventory, 아래쪽은 선택 아이템의 변환 가능한 목적지로 구성했다.
- 전체 인벤토리 모두 변환, 선택 아이템 모두 변환, rule별 1회 변환, rule별 모두 변환을 제공한다.
- 변환 성공 후 module state와 host state를 다시 불러와 UI를 갱신한다.
- `exitMode`에서는 남은 전용 자원이 있거나 rule이 없는 자원이 있으면 나가기 버튼을 막는다.
- `allowDeckTransfer`가 꺼진 일반 구역에서는 `deck-cargo`로 가는 임시 갑판 적재 rule을 숨기는 조건을 유지한다.

### 4. 항해 보드와 도착 섬 화면 연결 완성

- [x] route landing의 `screenPath`가 `/voyage/island/shell`로 자연스럽게 이동하는지 확인한다.
- [x] 항해 보드에서 현재 landing이 있는 경우 도착 섬 진입 버튼을 명확하게 보여준다.
- [x] 도착 섬에서 복귀하거나 이탈할 때 route landing 상태와 세관 gate 상태가 충돌하지 않게 한다.
- [x] 빈 바다, 폭풍, 보물섬, 캐릭터 섬은 아직 기획 후보로 두고 화면 fallback을 정리한다.

구현 결과:

- `App.tsx`에서 모듈 라우트를 실제 React Router에 렌더링하도록 연결했다.
- `/voyage/island/shell` 도착 섬 라우트가 e2e smoke 대상에 포함됐다.
- 항해 보드는 backend의 현재 landing을 다시 불러와 새로고침 후에도 도착 섬 진입 모달을 복구한다.
- `landing.screenPath`가 있으면 도착 모달 버튼을 `도착 섬으로 들어가기`로 표시한다.
- mission이 있는 landing은 도착 섬 진입 전에 `landing/clear`를 호출해 보상을 지급하고 이후 도착 섬 화면으로 이동한다.
- 도착 섬의 Pixi scene cleanup을 보수적으로 정리해 라우트 이동 중 runtime error가 나지 않게 했다.

### 5. 숙소 꾸미기 UI 연결

- [x] backend `POST /api/modules/lodge/furniture/purchase`를 frontend UI에 연결한다.
- [x] backend `POST /api/modules/lodge/rooms/furniture/toggle`을 frontend UI에 연결한다.
- [x] 빈 숙소 방도 꾸밀 수 있게 한다.
- [x] 숙소에 배치되어 있지 않은 Aidong의 방도 꾸밀 수 있게 한다.
- [x] 항해 중이거나 항구에 배치된 Aidong은 방 꾸미기는 가능하지만 케어, 밥주기, 옷입히기는 막는다.

구현 결과:

- `LodgeScene`을 방 선택 중심 구조로 정리했다.
- 숙소 가구 목록은 backend `GET /api/modules/lodge/config`의 `decor.csv` catalog를 사용한다.
- 가구 구매와 방별 배치/해제는 lodge 전용 backend API를 호출한다.
- `empty-room-1`, `empty-room-2`를 빈 방 roomId로 사용해 Aidong이 없는 방도 꾸밀 수 있게 했다.
- 항해 중 또는 항구 지원 배치 중인 Aidong은 방 꾸미기 tab만 사용할 수 있고, 케어/밥/옷 tab은 비활성화된다.
- `packages/modules/lodge/decor.csv`의 깨진 한글을 복구했다.
- 검증: `pnpm --filter frontend typecheck`, `pnpm check:live-smoke:local` 통과.

### 6. 선실 꾸미기 UI 연결

- [x] backend `POST /api/modules/ship/cabins/furniture/purchase`를 frontend UI에 연결한다.
- [x] backend `POST /api/modules/ship/cabins/furniture/toggle`을 frontend UI에 연결한다.
- [x] 빈 선실도 꾸밀 수 있게 한다.
- [x] 항구와 항해 중 배 메뉴 모두에서 선실 꾸미기 상태가 동일하게 보이게 한다.
- [x] 선실에 Aidong이 없을 때는 밥주기, 케어 버튼을 숨기거나 비활성화한다.

구현 결과:

- `HarborScene`과 `NavigationBoardScene`의 선실 꾸미기 dialog가 backend `GET /api/modules/ship/config`의 `cabinFurnitureItems` catalog를 사용한다.
- 선실 가구 구매와 배치/해제는 ship 전용 backend API를 호출한다.
- 빈 선실에서도 가구 배치/해제가 가능하다.
- 항구와 항해 중 배 메뉴가 같은 `shipStates.cabins`, `shipStates.cabinFurniture` 상태를 기준으로 선실 꾸미기를 표시한다.
- 선실에 Aidong이 없으면 밥주기와 케어 메뉴를 보여주지 않고, 선실 꾸미기만 가능하게 유지한다.
- `packages/modules/ship/decor.csv`의 깨진 한글을 복구했다.
- 검증: `pnpm --filter frontend typecheck`, `pnpm check:live-smoke:local` 통과.

### 7. 항구/항해 중 배 메뉴 정리

- [x] 항구에 있는 배와 항해 중인 배의 조작 가능 범위를 분리한다.
- [x] 출항 전 항구에서는 배 변경, 선실/갑판 배치, 출항 준비를 할 수 있다.
- [x] 출항 중 항구 화면에서는 배 변경, 선실/갑판 재배치, 재출항을 막는다.
- [x] 출항 중 항해 보드 하단에는 배 메뉴를 보여준다.
- [x] 항해 중 배 메뉴에서는 항해 중인 Aidong만 선실/갑판 배치 후보로 보여준다.
- [x] 항해 중 선실에서 Aidong을 비우면 자동으로 갑판으로 이동한다.

구현 결과:

- 항구 화면은 출항 전일 때만 배 변경, 선실/갑판 배치, 출항 메뉴를 보여준다.
- 출항 중 항구 화면은 현재 항해 정보와 보드 이동 버튼을 보여주고, 항구 지원 배치 메뉴만 사용할 수 있게 유지한다.
- 항해 보드 하단 배 메뉴는 현재 항해 중인 배의 선실/갑판 상태를 표시한다.
- 항해 중 배 메뉴의 배치 후보는 현재 배에 탄 Aidong으로 제한한다.
- 항해 중 선실에서 Aidong을 비우면 backend `context: voyage` 규칙에 따라 갑판으로 이동한다.
- 선실에서 갑판으로 이동한 Aidong이 deck capacity 밖 임시 슬롯에 들어가도 화면에서 사라지지 않도록, 실제 배정된 최대 슬롯 번호까지 갑판을 렌더링한다.
- 검증: `pnpm --filter frontend typecheck`, `pnpm check:live-smoke:local` 통과.

### 8. Ship/Lodge Inventory 화면 정리

- [x] 배 인벤토리에서 `shipInventory` 내용을 표시한다.
- [x] 배 인벤토리에 자원이 있으면 세관 또는 하역 버튼을 제공한다.
- [x] 숙소 인벤토리에서 `lodgeInventory` 내용을 표시한다.
- [x] ship -> lodge, ship -> global, lodge -> host customs 흐름을 UI에서 확인한다.
- [x] `shell-fragment`가 배에서 숙소로 내려가는지 확인한다.

구현 결과:

- 항구 화면의 배 인벤토리 dialog는 `shipInventory`와 legacy/reserved `cargo`를 함께 표시한다.
- 항해 중 배 메뉴에도 배 인벤토리 버튼과 세관 dialog를 추가했다.
- 배 인벤토리에 자원이 있으면 `sourceModule="ship"` 세관 dialog를 열 수 있다.
- 숙소 화면에 숙소 인벤토리 버튼과 dialog를 추가하고 `lodgeInventory`를 표시한다.
- 숙소 인벤토리에 자원이 있으면 `sourceModule="lodge"` 세관 dialog를 열 수 있다.
- 세관 처리 후 배/숙소 module state를 다시 불러와 UI를 갱신한다.
- backend live smoke에서 `ship-acorn-to-lodge`, `ship-sailcloth-to-host`, `lodge-aidong-ribbon-to-host`, `shell-fragment-to-ship` 흐름이 계속 검증된다.
- 검증: `pnpm --filter frontend typecheck`, `pnpm check:live-smoke:local` 통과.

### 9. PixiJS Destination Scene 연출 개선

- [x] node 전환 시 화면 즉시 갈라짐이나 어색함을 줄이고 transition을 강화한다.
- [x] hotspot hover/click 효과를 더 명확하게 만든다.
- [x] 배경 layer와 foreground layer의 깊이감을 보강한다.
- [x] 모바일 화면에서 canvas가 과도하게 잘리거나 텍스트를 가리지 않는지 확인한다.

구현 결과:

- `DestinationIslandPixiScene`에 원경 섬, 수평선 glow, foreground 암석/풀, vignette layer를 추가해 고정맵의 깊이감을 보강했다.
- node 전환 시 흰색 transition flash가 짧게 지나가도록 처리해 화면이 즉시 갈라지는 느낌을 줄였다.
- 파도, 입자, hotspot ring/core animation을 강화해 정적인 배경 위에 가벼운 생동감을 추가했다.
- hotspot DOM 버튼의 hover, active, 그림자, 테두리, 반응형 크기를 조정해 클릭 가능한 지점이 더 명확하게 보이도록 했다.
- 모바일 화면에서 섬 canvas가 너무 작게 눌리지 않도록 scene 최소 높이와 버튼 폭을 조정했다.
- 검증: `pnpm --filter frontend typecheck`, `pnpm check:live-smoke:local` 통과.

### 10. Rive Reaction Asset 연결 준비

- [x] 현재 fallback UI로 동작하는 Rive layer의 실제 `.riv` asset 연결 지점을 정리한다.
- [x] `VITE_DESTINATION_ISLAND_RIVE_SRC` 사용법을 문서화한다.
- [x] state machine input 이름 `hotspot`, `reward`, `mission`, `error`를 유지한다.
- [x] 실제 asset이 없을 때도 화면이 깨지지 않게 fallback을 유지한다.

구현 결과:

- `DestinationIslandRiveLayer`에 기본 state machine 이름 `DestinationIslandReaction`과 trigger input 이름 `hotspot`, `reward`, `mission`, `error`를 상수로 고정했다.
- 실제 `.riv` asset은 `DestinationIslandScreen`에서 `VITE_DESTINATION_ISLAND_RIVE_SRC` 환경변수로 주입한다.
- `src`가 없으면 현재 fallback badge UI를 그대로 표시해 실제 asset 제작 전에도 화면이 깨지지 않는다.
- `.cloud/44_destination_island_guide.md`에 Rive 연결 계약, 환경변수 예시, reaction 의미, fallback 원칙을 문서화했다.
- 검증: `pnpm --filter @idongworld/destination-shell-island typecheck`, `pnpm --filter frontend typecheck` 통과.

### 11. Decor/Item Catalog Audit 확장

- [x] `audit:module-items`의 customs와 items 정합성 검사를 계속 유지한다.
- [x] decor catalog 전용 audit 후보를 만든다.
- [x] `decor.csv`의 `itemId`, `cost`, `defaultOwned`, `placementScope`를 검사한다.
- [x] manifest에 `decor`가 선언된 모듈은 실제 파일이 존재해야 한다.
- [x] 구매/배치 API가 참조하는 decor item이 CSV에 존재하는지 확인한다.

구현 결과:

- 기존 `audit:module-items`는 item/customs 정합성 검사로 유지했다.
- 새 `tools/audit/checkModuleDecor.mjs`를 추가해 `manifest.ts`에 `decor`가 선언된 모듈을 찾고 `decor.csv` 파일 존재와 catalog 구조를 검사한다.
- decor audit은 `itemId`, `label`, `cost`, `defaultOwned`, `placementScope`, `description`, `phase` 필수 column을 확인한다.
- `cost`, `defaultOwned`는 0 이상의 정수여야 하며, `placementScope`는 현재 `lodge-room`, `ship-cabin`만 허용한다.
- root script에 `pnpm audit:module-decor`와 `pnpm audit:module-catalogs`를 추가했다.
- `.cloud/48_lodge_ship_decor_schema_policy.md`와 `packages/modules/BACKEND_GUIDE.md`에 decor audit 명령과 기준을 문서화했다.
- 검증: `pnpm audit:module-items`, `pnpm audit:module-decor`, `pnpm audit:module-catalogs` 통과.

### 12. 다음 작업 중 발견한 확인/오류 기록

- [ ] live smoke 또는 수동 QA에서 나온 오류를 이 문서에 즉시 추가한다.
- [ ] 오류 항목에는 재현 경로, 원인, 수정 파일, 검증 명령을 남긴다.
- [ ] 한글 깨짐이 보이면 영어로 회피하지 말고 UTF-8 한글로 복구한다.

## 우선순위

1. Live Smoke 검증
2. Destination Island 이탈 세관 Gate 연결
3. Customs UI UX 마무리
4. 항해 보드와 도착 섬 화면 연결 완성
5. 숙소/선실 꾸미기 UI 연결

## 보류 항목

- Atlas/S3/CloudFront 실제 signed URL provider 연동.
- Spine/Live2D 캐릭터 리깅.
- 선실/갑판 effect가 cargo capacity에 주는 보너스.
- `shipStates.cargo` 완전 제거 또는 cargo run 상세 구조 재사양 여부.
- `shipStates.cabins.__furnitureInventory` legacy migration.
- 보물섬, 폭풍, 캐릭터 섬의 상세 기획.

## 변경 기록

- **2026-06-08**: 2026-06-05 기획 변경 회의 가이드를 상위 입력으로 반영하면서 이 next_work를 취소 처리했다. 완료된 1~11번은 구현 이력으로 보존하고, 남은 12번과 이후 후속 순서는 `.cloud/51_next_work_2026-06-08.md`로 이관한다.
- **2026-06-05**: 2026-06-03 작업 보드 완료 이후 2차 next_work를 작성했다. 다음 순서는 live smoke, destination island 세관 gate, customs UX, 항해/도착 섬 화면 연결, 숙소/선실 꾸미기 UI 연결을 중심으로 잡았다.
- **2026-06-05**: `decorCatalog.ts`의 decor catalog 경로를 실제 `packages/modules/{moduleId}/decor.csv` 기준으로 수정했다.
- **2026-06-05**: `pnpm check:live-smoke:local` 명령과 `tools/checks/checkLiveSmokeLocal.mjs`를 추가했다. 이 명령은 local Mongo, backend, frontend를 띄운 뒤 module action smoke, Playwright e2e smoke, state-route runtime smoke를 순서대로 실행하고 자동 정리한다.
- **2026-06-05**: 1번 Live Smoke 검증을 완료했다. `pnpm check:state-route-runtime:local`과 `pnpm check:live-smoke:local`이 모두 통과했다.
- **2026-06-05**: 2번 Destination Island 이탈 세관 Gate를 구현했다. `destination-shell-island` 화면에서 남은 섬 전용 자원이 있으면 이탈 전 세관 dialog를 열고, `shell-fragment`를 배 인벤토리 또는 전역 코인으로 변환한 뒤 항해 보드로 나갈 수 있게 했다.
- **2026-06-05**: 3번 Customs UI UX를 정리했다. 공용 `CustomsTransferDialog`를 정상 한글, source inventory 상단 표시, target rule 하단 표시, 단일/전체 변환 액션 중심으로 재구성했다.
- **2026-06-05**: 4번 항해 보드와 도착 섬 화면 연결을 완료했다. 모듈 라우트를 `App.tsx`에 연결하고, 항해 보드에서 현재 landing 복구와 `/voyage/island/shell` 진입을 지원하며, 도착 섬 라우트를 e2e smoke 대상에 추가했다.
- **2026-06-05**: 5번 숙소 꾸미기 UI 연결을 완료했다. `LodgeScene`을 방 선택 중심으로 정리하고 lodge config, 가구 구매 API, 방별 가구 배치 API를 연결했다. 빈 방도 꾸밀 수 있으며, 항해 중/항구 지원 중인 Aidong은 방 꾸미기만 가능하고 케어/밥/옷 액션은 막는다.
- **2026-06-05**: 숙소 꾸미기 UI 변경 후 `pnpm --filter frontend typecheck`와 `pnpm check:live-smoke:local`을 실행해 통과를 확인했다. 현재 live smoke의 lodge action 범위에는 `furniture/purchase`, `rooms/furniture/toggle`이 아직 직접 포함되어 있지 않으므로 이후 11번 audit/smoke 확장 후보로 남긴다.
- **2026-06-05**: 6번 선실 꾸미기 UI 연결을 완료했다. 항구와 항해 중 배 메뉴의 선실 dialog가 `ship/decor.csv` 기반 `cabinFurnitureItems`를 사용하고, 선실 가구 구매/배치 API를 호출하도록 정리했다. 빈 선실은 꾸미기만 가능하고 Aidong이 있는 선실에서만 밥주기/케어 메뉴를 제공한다.
- **2026-06-05**: 선실 꾸미기 UI 변경 후 `pnpm --filter frontend typecheck`와 `pnpm check:live-smoke:local`을 실행해 통과를 확인했다.
- **2026-06-05**: 7번 항구/항해 중 배 메뉴 정리를 완료했다. 항구 화면은 출항 전 배 조작과 출항 중 항구 지원 배치를 분리하고, 항해 보드 하단 배 메뉴는 항해 중인 Aidong만 배치 후보로 사용한다. 선실에서 갑판으로 이동한 Aidong이 capacity 밖 임시 deck 슬롯에 들어가도 화면에 보이도록 실제 배정 슬롯 수를 기준으로 deck/cabin 렌더링을 보강했다.
- **2026-06-05**: 항구/항해 중 배 메뉴 변경 후 `pnpm --filter frontend typecheck`와 `pnpm check:live-smoke:local`을 실행해 통과를 확인했다.
- **2026-06-05**: 8번 Ship/Lodge Inventory 화면 정리를 완료했다. 항해 중 배 메뉴에 배 인벤토리와 ship 세관 dialog를 추가하고, 숙소 화면에 lodgeInventory 표시와 lodge 세관 dialog를 추가했다. 세관 처리 후 관련 module state를 다시 불러오도록 연결했다.
- **2026-06-05**: Ship/Lodge Inventory UI 변경 후 `pnpm --filter frontend typecheck`와 `pnpm check:live-smoke:local`을 실행해 통과를 확인했다.
- **2026-06-05**: 9번 PixiJS Destination Scene 연출 개선을 완료했다. 도착 섬 Pixi scene에 원경/전경 layer, transition flash, 파도/입자/hotspot animation을 추가하고 hotspot 버튼의 hover/click 표현과 모바일 배치를 보강했다.
- **2026-06-05**: PixiJS Destination Scene 변경 후 `pnpm --filter frontend typecheck`와 `pnpm check:live-smoke:local`을 실행해 통과를 확인했다.
- **2026-06-05**: 10번 Rive Reaction Asset 연결 준비를 완료했다. `DestinationIslandRiveLayer`의 state machine/input 계약을 상수화하고, `VITE_DESTINATION_ISLAND_RIVE_SRC` 환경변수 연결 방식과 fallback 원칙을 destination island guide에 문서화했다.
- **2026-06-05**: 11번 Decor/Item Catalog Audit 확장을 완료했다. 기존 `audit:module-items`는 item/customs 정합성 검사로 유지하고, 새 `audit:module-decor`와 묶음 명령 `audit:module-catalogs`를 추가해 manifest decor 선언과 `decor.csv` 필수 column, 비용, 기본 보유량, 배치 범위를 검증한다.
